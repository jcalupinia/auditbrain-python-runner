{
  "name": "auditbrain-e2e-portal-cliente",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:local": "cross-env E2E_BASE_URL=http://localhost:5174 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
