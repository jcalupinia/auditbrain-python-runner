# ICT 2025 — Diseño técnico

**Fecha:** 2026-05-30
**Autor:** Jorge Vinicio (Lider Audit-IA) + Claude
**Estado:** Spec aprobado por el usuario, listo para writing-plans
**Próximo paso:** Generar plan de implementación con `writing-plans`
**Construye sobre:** `docs/superpowers/specs/2026-05-28-portal-cliente-design.md` (portal cliente shell ya en producción)

---

## 0. Resumen ejecutivo

Construir la **primera herramienta de negocio del Portal Cliente de Audit Consulting Group**: una aplicación web que permite a un cliente externo armar incrementalmente su Anexo del Informe de Cumplimiento Tributario (ICT) 2025, subiendo documentos cuando los tiene disponibles, y descargar al final un Excel **100% idéntico al template oficial del SRI** con las celdas de input llenas y las fórmulas originales del template preservadas.

**UX clave:** una sola pantalla "dashboard" por proyecto ICT (similar al índice del Anexo), con botones de upload agrupados por anexo. Sesión persistente 90 días. Datos extraídos sobreviven a la eliminación de los archivos crudos (24h post-proceso por seguridad).

**Alcance MVP:** framework completo + **5 anexos** funcionando end-to-end (ÍNDICE, A1 Mapeo, A2 Ingresos, A3 Costos y Gastos, A9 Inventarios). Los 5 anexos restantes (A4, A5, A6, A7, A8) se agregan después en specs separados sobre el mismo framework.

**Insight clave**: tras analizar un ICT real ya lleno (ARCOLANDS CÍA. LTDA. 2024), confirmamos que A2 y A3 son tabulares con dimensiones fijas y fórmulas Excel preexistentes — significativamente más simples de lo estimado inicialmente. Por eso entran al MVP.

---

## 1. Decisiones tomadas

| # | Decisión | Valor | Razón |
|---|----------|-------|-------|
| 1 | UX del cliente | Dashboard único por proyecto ICT con uploads agrupados por anexo | Flujo realista: el cliente NO tiene todos los docs el mismo día |
| 2 | Persistencia de sesión | 90 días sin actividad | Ciclo anual de cumplimiento, espacio para iteración |
| 3 | Política de retención de archivos crudos | 24h post-procesado (heredado del portal) | Privacidad de datos sensibles |
| 4 | Política de retención de datos extraídos | 90 días en DB | Permite regenerar Excel sin re-subir |
| 5 | Política de retención del Excel generado | No se persiste, se regenera bajo demanda | Siempre refleja el estado actual de la sesión |
| 6 | Template del output | El Excel oficial SRI 2025 (semilla en repo) | Garantiza formato y fórmulas idénticas |
| 7 | Estrategia de llenado | Sobrescribir solo celdas de INPUT, preservar fórmulas y formato | Excel calcula las fórmulas al abrir, NO Python |
| 8 | F-101 format | PDF | Lo que el SRI emite a 100% de los clientes |
| 9 | Tolerancia a fallas de parseo | Estado por anexo: `empty / partial / ready` | Permite avance progresivo aún con datos incompletos |
| 10 | Alcance MVP | Framework + ÍNDICE + A1 + A2 + A3 + A9 (5 de 10 anexos) | Cubre 5 de los 7 anexos que un cliente típico marca como "Aplica SI" (validado con ARCOLANDS 2024) |
| 10b | Estrategia para A1 | Inserción dinámica de filas (cada casillero F-101 = 1 fila base + N subcuentas del balance) | Validado en ARCOLANDS: casillero 311 "Efectivo" tiene 6 subcuentas; A1 lleno crece de 40 a 197 filas |
| 10c | Estrategia para A3 | 9 bloques con misma estructura, solo llenar casilleros F-101 base | Las fórmulas de cálculo (porcentajes, totales) ya están en el template — Excel calcula solo |
| 11 | Integración con portal cliente | NO entra al `tool_registry` genérico; es módulo dedicado `ict/` con endpoints propios | El ICT requiere sesión persistente y estado, no encaja en el modelo "subir-procesar-descargar" del registry |
| 12 | Reutilización código existente | `find_casillero_value`, `find_periodo` de `aud/obligaciones_fiscales/cedulas/base.py` + patrón openpyxl del `excel_assembler.py` | Ya probado en producción |
| 13 | Catálogo casillero F-101 ↔ cuenta contable | Diccionario Python hardcoded (extensible a DB futuro) | El catálogo SRI es estable; iteramos rápido |
| 14 | Categoría en el catálogo cliente | `CUMPLIMIENTO_TRIBUTARIO` | Reemplaza el `STUB_ECHO` o convive con él |
| 15 | Auth | Mismas 3 capas del portal cliente: JWT+rol, device, sid | Heredado, sin cambios |

---

## 2. UX — Dashboard del ICT

### 2.1 Entrada al ICT

```
Cliente Portal → /catalog
        ↓
Click en tarjeta "Anexo ICT 2025 — Informe de Cumplimiento Tributario"
(categoría CUMPLIMIENTO_TRIBUTARIO)
        ↓
        - Si NO existe sesión activa: pantalla "Crear nueva sesión" (RUC, año, etc.)
        - Si EXISTE sesión activa: directo al Dashboard
```

### 2.2 Dashboard (pantalla principal del ICT)

```
┌────────────────────────────────────────────────────────────────────────────┐
│  📑 ICT 2025  ·  Empresa XYZ S.A.   [💾 Guardar] [📥 Descargar Excel]      │
├────────────────────────────────────────────────────────────────────────────┤
│  📋 Datos del contribuyente              [ ✏️ Editar ]                      │
│     RUC: 1234567890001                                                      │
│     Razón Social: Empresa XYZ S.A.                                          │
│     Ejercicio Fiscal: 2025                                                  │
│     N° Adhesivo: ABC-12345                                                  │
├────────────────────────────────────────────────────────────────────────────┤
│  📑 Anexos                                                                  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐     │
│  │ ÍNDICE                                       ✅ Auto-completado    │     │
│  │ Identificación + flag SI/NO por anexo                              │     │
│  └──────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐     │
│  │ A1 · Mapeo de la Declaración                 🟡 Parcial            │     │
│  │ Cruza casilleros F-101 con cuentas contables del Balance           │     │
│  │ ✅ Formulario 101 (cargado 15-Oct, 142 casilleros detectados)      │     │
│  │ ⚪ Balance de Comprobación                                          │     │
│  │     [ 📤 Subir Balance Excel ]                                      │     │
│  └──────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐     │
│  │ A9 · Inventarios                              ⚪ Pendiente          │     │
│  │ Casilleros 7001-7037 del F-101 + datos del Kardex                  │     │
│  │ ✅ Formulario 101 (reutilizado de A1)                              │     │
│  │ ⚪ Kardex (opcional)                                                │     │
│  │     [ 📤 Subir Kardex Excel ]                                       │     │
│  │     ℹ️ Si no subes Kardex, el anexo se llena solo con F-101         │     │
│  └──────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  ─── Próximamente ───                                                       │
│  A2 Ingresos · A3 Costos y Gastos · A4 Conciliación Ingresos               │
│  A5 Conciliación C&G · A6 Beneficios · A7 Crédito · A8 Comercio Exterior   │
│                                                                              │
│  ──────────────────────────────────────────────────────────                │
│  Progreso MVP: 1/3 anexos completos        Última actividad: hace 2 días   │
└────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 Comportamiento UI

| Acción | Reacción del sistema |
|--------|----------------------|
| Cliente sube F-101 | Procesa inmediatamente, extrae casilleros, marca ✅ en A1 y A9 (reutiliza el mismo F-101) |
| Cliente sube Balance | Procesa, extrae cuentas+saldos, A1 pasa a 🟡 si faltaba algo o ✅ si está completo |
| Cliente sube Kardex | Procesa, A9 pasa a ✅ |
| Cliente click "Descargar" | Genera Excel ICT al vuelo con todos los datos actuales y descarga |
| Cliente sale y vuelve mañana | Login → ve su dashboard exactamente como lo dejó |
| Cliente edita datos contribuyente | Modal de edición, ÍNDICE se regenera |
| Pasan 90 días sin actividad | Sesión marcada `expired`, datos preservados 30 días extra (recuperable por staff) |

### 2.4 Estados visuales de un anexo

| Estado | Indicador | Significado |
|--------|-----------|-------------|
| `empty` | ⚪ Pendiente | No se ha subido ningún input para este anexo |
| `partial` | 🟡 Parcial | Al menos un input subido, faltan otros para completar |
| `ready` | ✅ Completo | Todos los inputs requeridos están listos |
| `error` | ❌ Error | El parseo del último input falló (con mensaje claro) |
| `locked` | 🔒 Próximamente | Anexo no implementado todavía en este MVP |

---

## 3. Arquitectura

### 3.1 Vista de alto nivel

```
            Frontend (frontend-client)                  Backend (FastAPI)
            ───────────────────────────                 ─────────────────
            /tools/ICT_2025
                  ↓                                                       
            ICTDashboard.jsx ◄────────────────── GET /api/v1/client/ict/sessions/active
                  ↓
            [Sube F-101] ───────────────────► POST /api/v1/client/ict/sessions/{id}/anexos/A1/upload
                                                       │
                                                       ▼
            (estado actualiza en pantalla)     ┌─────────────────────────────────┐
                                                │  ict/service.upload_for_anexo() │
                                                │  1. Detecta tipo de doc        │
                                                │  2. Llama parser correcto      │
                                                │  3. Persiste extracted_data    │
                                                │  4. Actualiza ICTAnexo.status  │
                                                │  5. Borra archivo crudo (24h)  │
                                                └─────────────────────────────────┘

            [Descargar Excel] ──────────────► GET /api/v1/client/ict/sessions/{id}/download
                                                       │
                                                       ▼
                                                ┌─────────────────────────────────┐
                                                │ ict/service.generate_excel()    │
                                                │ 1. Carga template SRI seed     │
                                                │ 2. Para cada anexo ready:      │
                                                │    → filler.fill(wb, data)     │
                                                │ 3. NO recalcular (Excel lo hace│
                                                │    al abrir)                   │
                                                │ 4. Stream como descarga        │
                                                └─────────────────────────────────┘
```

### 3.2 Nuevos módulos en el backend

```
backend/app/ict/                              ← NEW MODULE
├── __init__.py
├── models.py                                 ← ICTSession + ICTAnexo (SQLAlchemy)
├── service.py                                ← create_session, upload_for_anexo, generate_excel
├── router.py                                 ← /api/v1/client/ict/* endpoints
├── schemas.py                                ← Pydantic models (request/response)
│
├── parsers/                                  ← Extractores de documentos
│   ├── __init__.py
│   ├── f101_pdf.py                           ← Extrae casilleros F-101 (regex + pdfplumber)
│   ├── f104_pdf.py                           ← Reutiliza extract_f104 de obligaciones_fiscales
│   ├── balance_excel.py                      ← Parser Balance de Comprobación
│   ├── facturacion_sri.py                    ← Parser reporte facturación electrónica SRI
│   └── kardex_excel.py                       ← Parser Kardex (opcional)
│
├── fillers/                                  ← Llenadores por anexo (preservan fórmulas)
│   ├── __init__.py
│   ├── base.py                               ← Protocol Filler + helpers comunes
│   ├── indice.py                             ← Llena pestaña ÍNDICE
│   ├── a1_mapeo.py                           ← Llena pestaña A1 (Mapeo) — con inserción dinámica de filas
│   ├── a2_ingresos.py                        ← Llena pestaña A2 (Ingresos, 3 cuadros)
│   ├── a3_costos_gastos.py                   ← Llena pestaña A3 (9 bloques de límites)
│   └── a9_inventarios.py                     ← Llena pestaña A9 (Inventarios)
│
├── cell_maps/                                ← Mapeos celda Excel → dato (1 por anexo)
│   ├── __init__.py
│   ├── indice.py
│   ├── a1.py
│   ├── a2.py                                 ← 3 cuadros con dimensiones fijas
│   ├── a3.py                                 ← 9 bloques de límites de deducibilidad
│   └── a9.py
│
├── mapping_catalog.py                        ← F-101 casillero ↔ prefijos cuenta contable
└── templates/
    └── ict_2025_template.xlsx                ← Semilla oficial SRI (con fórmulas)
```

### 3.3 Frontend additions

```
frontend-client/src/
├── ict/                                      ← NEW
│   ├── ICTLanding.jsx                        ← "No tienes sesión activa - crear nueva"
│   ├── ICTDashboard.jsx                      ← Pantalla principal (dashboard único)
│   ├── ICTAnexoCard.jsx                      ← Componente reutilizable por anexo
│   ├── ICTUploader.jsx                       ← Botón de upload con feedback inline
│   ├── ICTEditContribuyenteModal.jsx         ← Modal para editar RUC, razón social, etc.
│   └── ictApi.js                             ← Cliente HTTP específico para /ict endpoints
│
├── App.jsx                                   ← MODIFY: añadir ruta /tools/ICT_2025
└── catalog/ClientCatalog.jsx                 ← MODIFY: el tool ICT_2025 navega a /tools/ICT_2025 (no a ToolShell genérico)
```

### 3.4 Reutilización de código existente

| Nuevo módulo ICT | Reutiliza de... |
|------------------|-----------------|
| `parsers/f101_pdf.py` | `aud/obligaciones_fiscales/cedulas/base.py::find_casillero_value, find_periodo` |
| `parsers/*` patrón | `cedulas/f104_extractor.py` (pdfplumber + regex) |
| `fillers/*` openpyxl | patrón de `aud/obligaciones_fiscales/excel_assembler.py` |
| `service.py` file storage | `aud/obligaciones_fiscales/file_storage.py` (helpers `create_job_dir`, `save_input`, `delete_job_dir`) |
| Auth | Mismas 3 capas: `require_client_with_device` |
| Cleanup 24h archivos | Patrón del cleanup existente (extender para borrar archivos ICT manteniendo metadata) |
| Email notification (opcional) | `notifications/email.py` cuando el cliente complete los 3 anexos del MVP |

---

## 4. Modelos de datos

### 4.1 ICTSession (NEW)

```python
class ICTSession(Base):
    """Sesión persistente del proyecto ICT de un cliente.
    Vive 90 días sin actividad; preserva datos extraídos aún si los
    archivos originales se borraron a las 24h.
    """
    __tablename__ = "ict_sessions"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    user_id: Mapped[int] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"), index=True, nullable=False
    )
    ejercicio_fiscal: Mapped[str] = mapped_column(String(4), nullable=False)
    ruc: Mapped[str] = mapped_column(String(20), nullable=False)
    razon_social: Mapped[str] = mapped_column(String(255), nullable=False)
    numero_adhesivo: Mapped[str | None] = mapped_column(String(64), nullable=True)

    status: Mapped[str] = mapped_column(
        String(20), default="in_progress", nullable=False
    )  # "in_progress" | "completed" | "expired"

    created_at: Mapped[datetime.datetime] = mapped_column(DateTime, nullable=False)
    last_activity_at: Mapped[datetime.datetime] = mapped_column(DateTime, nullable=False)
    expires_at: Mapped[datetime.datetime] = mapped_column(DateTime, nullable=False)
    # Renueva expires_at = last_activity_at + 90 días en cada update
```

### 4.2 ICTAnexo (NEW)

```python
class ICTAnexo(Base):
    """Datos extraídos por anexo. Persiste 90 días con la sesión."""
    __tablename__ = "ict_anexos"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    session_id: Mapped[int] = mapped_column(
        ForeignKey("ict_sessions.id", ondelete="CASCADE"), index=True, nullable=False
    )
    anexo_code: Mapped[str] = mapped_column(String(16), nullable=False)
    # Valores válidos en MVP: "INDICE", "A1", "A9"
    # Valores futuros: "A2", "A3", "A4", "A5", "A6", "A7", "A8"

    status: Mapped[str] = mapped_column(String(20), default="empty", nullable=False)
    # "empty" | "partial" | "ready" | "error"

    extracted_data: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    # JSON dinámico por anexo:
    # A1: {"f101": {"311": 5000, "312": 1200, ...}, "balance": {"1.1.1.01.01": {nombre: "Caja", saldo: 4500}, ...}}
    # A9: {"f101": {"7001": 10000, "7010": 8000, ...}, "kardex": {nota: "no provisto"}}
    # INDICE: {"anexos_aplica": {"A1": "SI", "A9": "SI", "A2": "NO", ...}}

    warnings: Mapped[list[str] | None] = mapped_column(JSON, nullable=True)
    # ["Casillero 828 no detectado en el PDF F-101"]

    uploaded_files: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    # Metadata de los archivos subidos (no el contenido):
    # {"f101": {filename: "F101_2025.pdf", size: 524288, uploaded_at: "2026-..."}}

    last_updated_at: Mapped[datetime.datetime] = mapped_column(DateTime, nullable=False)

    __table_args__ = (
        UniqueConstraint("session_id", "anexo_code", name="uq_session_anexo"),
    )
```

### 4.3 Sin cambios en otras tablas

`User`, `ClientDevice`, `ToolJob` quedan como están. El ICT es independiente del modelo `ToolJob` genérico (porque su flujo es distinto: persistente, no one-shot).

---

## 5. Endpoints HTTP

Todos prefijados `/api/v1/client/ict/`, protegidos por `require_client_with_device`.

| Método | Ruta | Qué hace |
|--------|------|----------|
| `POST` | `/sessions` | Crear nueva sesión ICT (`ruc`, `razon_social`, `ejercicio_fiscal`, `numero_adhesivo?`). Si el cliente ya tiene una `in_progress`, devuelve esa. |
| `GET` | `/sessions/active` | Devuelve la sesión activa del cliente o 404 si no hay. |
| `GET` | `/sessions/{id}` | Detalle: cliente_data + lista de anexos con su estado, warnings, archivos subidos. |
| `PATCH` | `/sessions/{id}` | Actualizar datos del contribuyente (razón social, adhesivo, etc.). |
| `POST` | `/sessions/{id}/anexos/{anexo_code}/upload` | Multipart: campo `slot_name` (ej. "f101", "balance", "kardex") + archivo. Procesa al instante. |
| `DELETE` | `/sessions/{id}/anexos/{anexo_code}/upload/{slot_name}` | Quita un archivo de un slot (reset el extracted_data parcial). |
| `GET` | `/sessions/{id}/download` | Genera Excel ICT al vuelo + StreamingResponse. |
| `DELETE` | `/sessions/{id}` | Marca la sesión como `expired` (cliente "termina" o "descarta"). |

---

## 6. Flujo de procesamiento por anexo

### 6.1 Mapping celda Excel → dato (cell_maps)

Cada anexo declara un dict que dice qué celda del template recibe qué dato.

**Ejemplo simplificado `cell_maps/a1.py`:**

```python
A1_SHEET = "MAPEO DE LA DECLARACIÓN A1"

# Encabezado del anexo (datos del cliente)
A1_HEADER_MAP = {
    "C5":  ("session", "razon_social"),
    "C6":  ("session", "ruc"),
    "C7":  ("session", "ejercicio_fiscal"),
}

# Casilleros F-101 que van en columna C (Valor declarado)
# Estructura: {fila_excel: numero_casillero}
A1_CASILLEROS_F101 = {
    15: "311",  # Efectivo y equivalentes
    16: "312",
    17: "313",
    # ... ~140 casilleros más
}

# Cuentas contables que van en columnas D-F (código, nombre, saldo)
# El catálogo dice qué prefijos contables pertenecen a cada casillero
# (ver mapping_catalog.py). Esto se calcula en tiempo de fill.
```

**Ejemplo `cell_maps/a9.py`:**

```python
A9_SHEET = "INVENTARIOS A9"

A9_HEADER_MAP = {
    "C3":  ("session", "razon_social"),
    "C4":  ("session", "ruc"),
    "C5":  ("session", "ejercicio_fiscal"),
    "C6":  ("session", "numero_adhesivo"),
}

# Casilleros fijos A9 (9 casilleros del F-101)
A9_CASILLEROS = {
    18: "7001",  # INVENTARIO INICIAL DE BIENES NO PRODUCIDOS
    19: "7010",  # INVENTARIO FINAL DE BIENES NO PRODUCIDOS
    20: "7013",  # INVENTARIO INICIAL DE MATERIA PRIMA
    21: "7022",  # INVENTARIO FINAL DE MATERIA PRIMA
    22: "7025",  # INVENTARIO INICIAL DE PRODUCTOS EN PROCESO
    23: "7028",  # INVENTARIO FINAL DE PRODUCTOS EN PROCESO
    24: "7031",  # INVENTARIO INICIAL PRODUCTOS TERMINADOS
    25: "7034",  # INVENTARIO FINAL DE PRODUCTOS TERMINADOS
    26: "7037",  # AJUSTES
}

# Columna C: valor declarado del F-101
# Columnas D-G: datos del kardex (código, forma valoración, cantidad, costo)
```

### 6.2 Filler protocol

```python
# fillers/base.py

from typing import Protocol
from openpyxl import Workbook


class Filler(Protocol):
    anexo_code: str

    def fill(
        self,
        workbook: Workbook,
        session_data: dict,           # {"razon_social", "ruc", "ejercicio_fiscal", "numero_adhesivo"}
        anexo_data: dict,             # ICTAnexo.extracted_data
    ) -> dict:
        """Llena las celdas de input del anexo. Devuelve {filled_cells: int, warnings: list[str]}.

        IMPORTANTE: solo escribe celdas que están en el cell_map del anexo.
        NUNCA toca celdas con fórmula. Excel calcula las fórmulas al abrir.
        """
        ...
```

### 6.3 Ejemplo: fillers/a1_mapeo.py

```python
from backend.app.ict.cell_maps.a1 import A1_SHEET, A1_HEADER_MAP, A1_CASILLEROS_F101
from backend.app.ict.mapping_catalog import F101_TO_BALANCE_PREFIXES


class A1Filler:
    anexo_code = "A1"

    def fill(self, workbook, session_data, anexo_data):
        ws = workbook[A1_SHEET]
        warnings = []
        filled = 0

        # 1. Llenar encabezado
        for cell, (source, key) in A1_HEADER_MAP.items():
            value = session_data.get(key, "")
            ws[cell] = value
            filled += 1

        # 2. Llenar casilleros F-101 (columna C)
        f101_data = anexo_data.get("f101", {})
        for row, casillero in A1_CASILLEROS_F101.items():
            value = f101_data.get(casillero)
            if value is not None:
                ws[f"C{row}"] = value
                filled += 1
            else:
                warnings.append(f"Casillero F-101 {casillero} no detectado")

        # 3. Llenar cuentas contables (columnas D-F)
        # Para cada casillero, buscar las cuentas del balance que matchen
        balance_data = anexo_data.get("balance", {})  # {código: {nombre, saldo}}
        for row, casillero in A1_CASILLEROS_F101.items():
            prefijos = F101_TO_BALANCE_PREFIXES.get(casillero, [])
            matched_cuentas = [
                (codigo, cuenta) for codigo, cuenta in balance_data.items()
                if any(codigo.startswith(p) for p in prefijos)
            ]
            if matched_cuentas:
                # El template tiene N filas en blanco para sub-cuentas
                # Llenamos secuencialmente desde la fila base
                for offset, (codigo, cuenta) in enumerate(matched_cuentas):
                    target_row = row + offset
                    ws[f"D{target_row}"] = codigo
                    ws[f"E{target_row}"] = cuenta["nombre"]
                    ws[f"F{target_row}"] = cuenta["saldo"]
                    filled += 3

        return {"filled_cells": filled, "warnings": warnings}
```

### 6.4 Generación del Excel final

```python
# service.py

def generate_excel_for_session(db, *, session_id: int) -> bytes:
    session = db.get(ICTSession, session_id)
    if not session:
        raise ValueError("Session not found")

    anexos = db.execute(
        select(ICTAnexo).where(ICTAnexo.session_id == session_id)
    ).scalars().all()

    # Cargar template oficial SRI (con fórmulas)
    template_path = Path(__file__).parent / "templates" / "ict_2025_template.xlsx"
    wb = load_workbook(template_path, data_only=False, keep_links=True)

    session_data = {
        "razon_social": session.razon_social,
        "ruc": session.ruc,
        "ejercicio_fiscal": session.ejercicio_fiscal,
        "numero_adhesivo": session.numero_adhesivo or "",
    }

    # Llenar cada anexo que tenga datos
    fillers = {
        "INDICE": IndiceFiller(),
        "A1": A1Filler(),
        "A9": A9Filler(),
    }
    for anexo in anexos:
        filler = fillers.get(anexo.anexo_code)
        if filler and anexo.extracted_data:
            filler.fill(wb, session_data, anexo.extracted_data)

    # NO recalcular. Excel calcula al abrir.
    # Stream a bytes
    from io import BytesIO
    buf = BytesIO()
    wb.save(buf)
    buf.seek(0)
    return buf.read()
```

---

## 7. Parseo de documentos

### 7.1 F-101 PDF

Reutiliza `find_casillero_value` y `find_periodo` de `aud/obligaciones_fiscales/cedulas/base.py`.

```python
# ict/parsers/f101_pdf.py

from io import BytesIO
import pdfplumber
from backend.app.aud.obligaciones_fiscales.cedulas.base import (
    find_casillero_value, find_periodo,
)

# Todos los casilleros del F-101 que nos interesan en el MVP
# A1 necesita ~140 casilleros (todo el formulario)
# A9 necesita 9 casilleros específicos
ALL_F101_CASILLEROS = [
    # Bloque Estado Situación Financiera
    "311", "312", "313", "314", "315", "316", "317", "318", "319", "320",
    # ... muchos más
    # Bloque Inventarios A9
    "7001", "7010", "7013", "7022", "7025", "7028", "7031", "7034", "7037",
    # ... etc
]


def parse_f101(pdf_bytes: bytes) -> dict:
    """Devuelve {periodo, casilleros: {num: valor}, errores: []}."""
    try:
        with pdfplumber.open(BytesIO(pdf_bytes)) as pdf:
            text = "\n".join(p.extract_text() or "" for p in pdf.pages)
    except Exception as e:
        return {"periodo": None, "casilleros": {}, "errores": [f"PDF inválido: {e}"]}

    if not text.strip():
        return {"periodo": None, "casilleros": {}, "errores": ["PDF sin texto extraíble (¿escaneado?)"]}

    periodo = find_periodo(text)
    casilleros = {}
    errores = []
    for num in ALL_F101_CASILLEROS:
        v = find_casillero_value(text, num)
        if v is not None:
            casilleros[num] = v

    return {"periodo": periodo, "casilleros": casilleros, "errores": errores}
```

### 7.2 Balance de Comprobación Excel

```python
# ict/parsers/balance_excel.py

from io import BytesIO
from openpyxl import load_workbook


def parse_balance(excel_bytes: bytes) -> dict:
    """Lee Balance de Comprobación. Devuelve {cuentas: {código: {nombre, saldo}}, errores}.

    Asume columnas: Código | Nombre | Saldo Final (otros formatos requieren mapeo flexible).
    Detecta automáticamente la fila de encabezado por keywords.
    """
    wb = load_workbook(BytesIO(excel_bytes), data_only=True)
    ws = wb.active

    # Detectar encabezado (busca fila con "código" y "saldo" / "monto")
    header_row = _find_header_row(ws)
    if header_row is None:
        return {"cuentas": {}, "errores": ["No se detectó fila de encabezado. Esperaba 'Código' y 'Saldo'."]}

    # Identificar índices de columnas
    col_codigo, col_nombre, col_saldo = _identify_columns(ws, header_row)
    if not all((col_codigo, col_saldo)):
        return {"cuentas": {}, "errores": ["No se identificaron columnas obligatorias."]}

    cuentas = {}
    errores = []
    for row in ws.iter_rows(min_row=header_row + 1, values_only=True):
        codigo = str(row[col_codigo]).strip() if row[col_codigo] else None
        if not codigo:
            continue
        nombre = str(row[col_nombre]).strip() if col_nombre is not None and row[col_nombre] else ""
        try:
            saldo = float(row[col_saldo]) if row[col_saldo] is not None else 0.0
        except (ValueError, TypeError):
            errores.append(f"Saldo inválido en fila código={codigo}")
            continue
        cuentas[codigo] = {"nombre": nombre, "saldo": saldo}

    return {"cuentas": cuentas, "errores": errores}
```

### 7.3 Kardex Excel

```python
# ict/parsers/kardex_excel.py

def parse_kardex(excel_bytes: bytes) -> dict:
    """Lee Kardex. Devuelve {items: [{codigo, descripcion, forma_valoracion, cantidad, costo_total}], errores}."""
    # Formato esperado del kardex puede variar mucho entre ERPs.
    # MVP: asume columnas estándar. Mejora futura: configuración por cliente.
    # Si el formato no coincide, devuelve errores claros para guiar al cliente.
    ...
```

---

## 8. Manejo de errores

### 8.1 Errores de upload

| Caso | HTTP | Mensaje al cliente | Acción |
|------|------|--------------------|--------|
| Archivo > 50 MB | 413 | "Archivo excede 50 MB" | No procesa, no toca el anexo |
| MIME no permitido | 415 | "Tipo no permitido para slot '{slot}'" | No procesa |
| PDF F-101 escaneado sin OCR | 200 (con error_partial) | "PDF parece escaneado. Sube versión digital o aplica OCR." | Anexo queda en `error` |
| Balance sin columnas esperadas | 200 (con error_partial) | "No se detectó la columna 'Código' en el Balance." | Anexo queda en `error` |
| F-101 con casilleros faltantes | 200 (parcial) | "F-101 procesado. 142 casilleros detectados, 8 no encontrados (listados en warnings)" | Anexo pasa a `partial` con warnings |

### 8.2 Errores de generación del Excel

| Caso | Comportamiento |
|------|----------------|
| Template SRI corrupto o ausente | 500 + alerta crítica al staff. NO debería pasar (lo testeamos en CI). |
| Anexo con extracted_data malformado | El filler skipea ese campo, agrega warning al log. El Excel se genera con celda vacía. |
| openpyxl rompe al guardar | 500. Logueamos stack trace. Cliente ve "Error técnico, intente más tarde". |

### 8.3 Errores de sesión

| Caso | Comportamiento |
|------|----------------|
| Cliente intenta crear 2ª sesión `in_progress` | 200 con la sesión existente (idempotente). |
| Cliente accede a sesión que no es suya | 403. Log de auditoría. |
| Cliente accede a sesión `expired` | 410 Gone + sugerencia "Su sesión expiró. Crear nueva". |

### 8.4 Política de mensajes

- Sin jerga técnica (no stack traces al cliente)
- Cada error dice: qué pasó + qué hacer + opción de soporte si aplica
- Logs detallados al staff

---

## 9. Testing

### 9.1 Unit tests

| Módulo | Cobertura crítica |
|--------|-------------------|
| `parsers/f101_pdf.py` | Casilleros extraídos correctamente de PDF de prueba |
| `parsers/balance_excel.py` | Maneja columnas en orden distinto, columnas faltantes, saldos negativos |
| `parsers/kardex_excel.py` | Formatos típicos (idempotent) |
| `fillers/a1_mapeo.py` | Llena celdas correctas, NO toca celdas con fórmula |
| `fillers/a9_inventarios.py` | Llena los 9 casilleros, maneja kardex ausente |
| `fillers/indice.py` | Marca SI/NO según anexos con `status=ready` |
| `service.generate_excel_for_session` | Genera Excel con formato preservado, fórmulas intactas |

### 9.2 Integration tests

| Test | Qué valida |
|------|-----------|
| `test_full_flow_a1_with_real_f101` | Sube F-101 + Balance reales → A1 queda `ready` con datos correctos |
| `test_a9_without_kardex_partial` | Solo F-101 → A9 queda `partial` con warning de kardex |
| `test_download_preserves_formulas` | Generar Excel, abrirlo con `data_only=False`, confirmar que `.value` de celdas con fórmula NO fue tocado |
| `test_session_isolation` | Cliente A no ve sesión de cliente B (403) |
| `test_session_expires_after_90_days` | Touch `last_activity_at`, advance time, verify `expired` |

### 9.3 E2E (Playwright, contra deploy)

1. Cliente login → `/tools/ICT_2025` → "Crear nueva sesión" → llena RUC, etc.
2. Dashboard aparece con ÍNDICE ✅ y A1, A9 ⚪
3. Sube F-101 → A1 y A9 pasan a 🟡 (parcial)
4. Sube Balance → A1 pasa a ✅
5. Click "Descargar" → recibe .xlsx
6. Reabre el archivo → confirma que datos están + fórmulas funcionan

### 9.4 Cobertura mínima

| Capa | Mínimo |
|------|--------|
| `ict/service.py` | 90% |
| `ict/parsers/*` | 85% |
| `ict/fillers/*` | 90% (incluye test de preservación de fórmulas) |
| `ict/router.py` | 80% (incluye tests de auth) |
| Frontend ICT components | 70% |

---

## 10. Fuera de scope (van en specs separados después del MVP)

| Anexo | Inputs adicionales requeridos | Estimación |
|-------|------------------------------|------------|
| **A4 Conciliación Ingresos** | F-101, Balance, Libro Mayor cuentas exentas | 2-3 días |
| **A5 Conciliación C&G** (5 cuadros + prorrateo) | F-101, Libro Mayor cuentas no deducibles | 4-6 días |
| **A6 Beneficios** (3 cuadros) | F-101, contratos inversión PDF, resoluciones | 3-5 días |
| **A7 Crédito** (matrices IR + ISD) | F-101 año actual + 3 anteriores, F-108, resoluciones SRI | 3-5 días |
| **A8 Comercio Exterior** (3 tablas) | F-103, ATS/REOC XML, certificados residencia fiscal | 5-7 días (único que requiere XML parsing) |

Cada anexo se agrega como:
- 1 entry en `fillers/` + 1 entry en `cell_maps/`
- Posiblemente nuevos parsers si el doc no fue procesado antes
- 0 cambios en el framework central

**Total para completar los 10 anexos**: ~4-5 semanas adicionales tras el MVP.

---

## 11. Riesgos

| Riesgo | Probabilidad | Impacto | Mitigación |
|--------|--------------|---------|------------|
| Template SRI cambia mid-año fiscal | Baja | Alto | Versionamos el template (`ict_2025_template_v1.xlsx`); cell_maps por versión |
| F-101 viene escaneado sin texto | Media | Medio | Detección + mensaje claro al cliente. OCR opcional como mejora futura. |
| Balance del cliente con formato no estándar | Alta | Medio | Parser tolerante, detección automática de columnas. Si falla: mensaje accionable. |
| openpyxl rompe formato visual al guardar | Baja | Alto | Test obligatorio en CI: cargar template, no modificar, guardar, hash debe coincidir (o muy cercano). |
| Casillero F-101 desaparece entre años | Baja | Bajo | Filler agrega warning, no rompe. Cell_map fácilmente actualizable. |
| Catálogo casillero↔cuenta incorrecto | Media | Medio | Catálogo en código fuente, peer review; futuro: override por cliente |
| Cliente sube docs y al regenerar Excel falla | Baja | Alto | extracted_data persiste 90 días; regenerable infinitas veces. Si falla, log + alerta staff. |
| Sesiones acumulan datos sensibles | Media | Alto | Cron que marca expired a 90 días, hard delete a 120 días, alerta al cliente por email a 80 días. |

---

## 12. Próximos pasos

1. Usuario revisa este spec y aprueba.
2. Invocar `writing-plans` para plan detallado.
3. Implementar:
   - Backend: models, parsers, fillers, service, router, tests
   - Frontend: ICTDashboard + componentes auxiliares
   - Integración con catálogo del portal cliente
4. QA contra un F-101 + Balance + Kardex reales (proporcionados por el usuario)
5. Deploy a Render
6. Smoke test end-to-end con un cliente piloto
7. Iterar: agregar A4 (siguiente spec, primer anexo post-MVP)

---

## Apéndice A — Cell maps detallados

(Los cell_maps completos para INDICE, A1, A9 se generan durante implementación leyendo el template SRI celda por celda. Aquí va el esqueleto; los valores exactos de filas/columnas se confirman al implementar consultando el archivo `Anexo ICT ejercicio fiscal 2025.xlsx`.)

### A.1 ÍNDICE

```python
# cell_maps/indice.py — derivado del ARCOLANDS ya lleno
INDICE_SHEET = "INDICE"

INDICE_HEADER_MAP = {
    "C3":  ("session", "razon_social"),
    "C4":  ("session", "ruc"),
    "C5":  ("session", "ejercicio_fiscal"),
}

# Tabla de anexos: columna J = "APLICA (SI/NO)"
# Se llena dinámicamente: SI si el anexo tiene status='ready' o 'partial', NO si está 'empty'
INDICE_APLICA_MAP = {
    10: "A1",
    12: "A2_C1",     # Cuadro No. 1 (Ingresos ordinarios)
    13: "A2_C2",     # Cuadro No. 2 (IVA vs Facturación)
    14: "A2_C3",     # Cuadro No. 3 (Conciliación)
    16: "A3",
    19: "A4_C1",     # NO en MVP
    20: "A4_C2",     # NO en MVP
    22: "A5",        # NO en MVP
    25: "A6_C1",     # NO en MVP
    26: "A6_C2",     # NO en MVP
    27: "A6_C3",     # NO en MVP
    29: "A7_C1",     # NO en MVP
    30: "A7_C2",     # NO en MVP
    32: "A8_C1",     # NO en MVP
    33: "A8_C2",     # NO en MVP
    35: "A9",
}
```

### A.2 A1 — Patrón de inserción dinámica de filas

**Observación del ARCOLANDS real**: cada casillero F-101 en A1 tiene:
- 1 fila base con `casillero | nombre | valor_declarado | (cuenta base) | (saldo base) | (diff)`
- N filas hijas con subcuentas adicionales `| | | (cuenta) | (nombre) | (saldo) | |`

**Ejemplo casillero 311 EFECTIVO Y EQUIVALENTES** ($5,008,023.09 declarado):

```
r13: 311 | EFECTIVO Y EQUIVALENTES AL EFECTIVO | 5008023.09 | 1.1.01.01.01 | CAJA CHICA UIO | 300 | 0
r14:     |                                     |            | 1.1.01.01.02 | CAJA CHICA SSFD | 1000 |
r15:     |                                     |            | 1.1.01.01.04 | CAJA CHICA MDC-40 | 100 |
r16:     |                                     |            | 1.1.01.02.01 | BANCO PICHINCHA | 280330.14 |
r17:     |                                     |            | 1.1.01.02.02 | BANCO INTERNACIONAL | 3632210.06 |
r18:     |                                     |            | 1.1.01.02.03 | BANCO INTERNACIONAL CONTRATO | 1094082.89 |
```

**Algoritmo del A1Filler:**

```python
def fill(self, workbook, session_data, anexo_data):
    ws = workbook[A1_SHEET]
    f101_data = anexo_data.get("f101", {})          # {casillero: valor}
    balance_data = anexo_data.get("balance", {})    # {codigo: {nombre, saldo}}

    # Empezamos en la fila base del primer casillero (ej. fila 13)
    current_row = A1_FIRST_DATA_ROW  # ej. 13

    for casillero, casillero_nombre in F101_A1_CASILLEROS:
        valor_declarado = f101_data.get(casillero, 0)
        prefijos = F101_TO_BALANCE_PREFIXES.get(casillero, [])

        # Buscar subcuentas matching del balance
        matching_cuentas = sorted([
            (codigo, info) for codigo, info in balance_data.items()
            if any(codigo.startswith(p) for p in prefijos)
        ])

        # Escribir fila base
        ws[f"A{current_row}"] = casillero
        ws[f"B{current_row}"] = casillero_nombre
        ws[f"C{current_row}"] = valor_declarado

        # Si hay subcuentas, primera va en la fila base
        if matching_cuentas:
            codigo, info = matching_cuentas[0]
            ws[f"D{current_row}"] = codigo
            ws[f"E{current_row}"] = info["nombre"]
            ws[f"F{current_row}"] = info["saldo"]
            # Columna G ya tiene fórmula =F-C (no la tocamos)

            # Resto de subcuentas requieren insert_rows
            for offset, (codigo, info) in enumerate(matching_cuentas[1:], start=1):
                ws.insert_rows(current_row + offset)
                ws[f"D{current_row + offset}"] = codigo
                ws[f"E{current_row + offset}"] = info["nombre"]
                ws[f"F{current_row + offset}"] = info["saldo"]

            current_row += len(matching_cuentas)  # avanzar tras subcuentas
        else:
            current_row += 1
```

### A.3 A2 — 3 cuadros con dimensiones fijas

**Cuadro 1 (Ingresos Ordinarios)** — 8 filas fijas × 4 columnas (tarifa IVA):

```python
# cell_maps/a2.py
A2_SHEET = "INGRESOS A2"

A2_CUADRO1_MAP = {
    # (fila, columna): (source, key)
    # columna B = Tarifa 0%, C = Tarifa diferente 0%, D = Exportaciones, E = Otros
    (14, "B"): ("balance_ingresos", "venta_bienes_t0"),
    (14, "C"): ("balance_ingresos", "venta_bienes_tdiff"),
    (14, "D"): ("balance_ingresos", "venta_bienes_export"),
    (14, "E"): ("balance_ingresos", "venta_bienes_otros"),
    (15, "B"): ("balance_ingresos", "servicios_t0"),
    # ... 8 filas x 4 columnas
    # NO tocar columna F (=B+C+D+E) — fórmula del template
}

A2_CUADRO2_MAP = {
    # 10 filas × ~12 columnas (IVA mensual + facturación electrónica + física + diferencia)
    # Datos provienen de la suma de 12 F-104 + reporte facturación SRI
}

A2_CUADRO3_MAP = {
    # 11 filas: conciliación IVA → IR
    # Mayoría son fórmulas; nosotros llenamos solo los inputs de NIIF/ajustes
}
```

### A.4 A3 — 9 bloques de límites de deducibilidad (patrón repetitivo)

**Estructura de cada bloque** (validado con ARCOLANDS):

```
[NOMBRE DEL BLOQUE] (ej. "GASTOS DE GESTIÓN")
[Cálculo del valor máximo de deducibilidad]

| No. Casillero | Descripción | Referencia | Valor | Obs |
|---------------|-------------|------------|-------|-----|
| 7992          | Total gastos declarados | {1} | 1531596.16 |  |
| 7185          | Gastos de gestión declarados | {2} | 25337.71 |  |
| -             | Total gastos generales = {1-2} | {3}={1-2} | 1506258.45 (fórmula) |  |
| -             | Porcentaje máximo (2%) | {4} | 0.02 (CONSTANTE template) |  |
| -             | Valor máximo deducibilidad = {3*4} | {5}={3*4} | 30125.17 (fórmula) |  |
|               |             |            |       |     |
| 7185          | Gastos de gestión declarados | {6} | 25337.71 |  |
| 7186          | Gastos no deducibles | {7} | 0 |  |
| -             | Gastos deducibles = {6-7} | {8}={6-7} | 25337.71 (fórmula) |  |
| -             | Diferencia = {8-5} | {9}={8-5} | 0 (fórmula, debe ser ≤ 0) |  |
```

**El filler de A3 solo necesita escribir los valores de casilleros F-101** (los `{1}`, `{2}`, `{6}`, `{7}` de cada bloque). El resto son fórmulas del template (`{3}={1-2}`, etc.) que Excel calcula sola.

```python
# cell_maps/a3.py
A3_SHEET = "COSTOS  GASTOS A3"

# Cada bloque: lista de (fila, casillero_F101)
# El % (col E como {4}) ya está hardcoded en el template
A3_BLOQUES = {
    "gastos_gestion": {
        "rows_casilleros": [
            (15, "7992"),  # Total gastos declarados
            (16, "7185"),  # Gastos de gestión declarados
            (21, "7185"),  # Gastos declarados (repetido en sección abajo)
            (22, "7186"),  # No deducibles
        ],
    },
    "gastos_viaje": {
        "rows_casilleros": [
            (32, "6999"),  # Total Ingresos
            (33, "804"),   # Dividendos exentos
            (34, "805"),
            (35, "812"),
            (36, "1116"),
            (37, "828"),
            (38, "834"),
            (39, "1117"),
            (40, "829"),
            (41, "835"),
            (46, "7182"),  # Gastos de viaje
            (47, "7183"),  # No deducibles
        ],
    },
    # ... 7 bloques más
}
```

### A.5 A9 — 9 casilleros fijos + datos kardex

Ver sección 6.1 arriba. Confirmado con ARCOLANDS: 9 casilleros (7001, 7010, 7013, 7022, 7025, 7028, 7031, 7034, 7037) en filas fijas (27-35).

---

## Apéndice B — Stack técnico

| Capa | Tecnología |
|------|------------|
| Backend | FastAPI 0.115+ |
| ORM | SQLAlchemy 2.0 |
| DB | PostgreSQL (Render) + SQLite (tests) |
| PDF parsing | pdfplumber (ya en el proyecto) |
| Excel parsing | openpyxl (ya en el proyecto) |
| Excel write w/ formulas | openpyxl `data_only=False, keep_links=True` |
| Frontend | React 18 + Vite (frontend-client) |
| Auth | JWT + device binding + sid (3 capas portal cliente) |
| Tests | pytest + httpx + Playwright |
| Deploy | Render (Web Service backend + Static Site frontend) |
