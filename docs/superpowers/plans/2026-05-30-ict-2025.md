# ICT 2025 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the **Anexo ICT 2025 automation tool** as the first business module of the Portal Cliente. Clients upload their tax documents (F-101 PDF, Balance Excel, F-104, ATS XML, etc.) and download a 100% SRI-compliant Excel with all 10 anexos auto-filled. Persistent 90-day session per client; raw files deleted at 24h; extracted data retained.

**Architecture:** New module `backend/app/ict/` with: (1) parsers per document type, (2) fillers per anexo that preserve template formulas via openpyxl `data_only=False`, (3) cell_maps that declare which Excel cells receive which data, (4) `ICTSession` + `ICTAnexo` DB models for persistence. Frontend: new `frontend-client/src/ict/` with dashboard UI showing upload buttons grouped by anexo. Reuses portal cliente auth (3 layers: JWT+role, device, sid) and `obligaciones_fiscales` parser patterns (`find_casillero_value`, `extract_f104`).

**Tech Stack:** FastAPI · SQLAlchemy 2.0 · PyJWT · pdfplumber · openpyxl · React 18 · Vite · pytest · Playwright · Render

**Spec:** `docs/superpowers/specs/2026-05-30-ict-2025-design.md` (797 lines, approved)

**Scope note:** This plan covers the FULL 10-anexo MVP. Fases 0-3 are written in full TDD detail. Fases 4-9 are outlined with acceptance criteria + references to patterns established in Fases 0-3; their full task content is expanded JUST-IN-TIME as we begin each phase, leveraging the framework and patterns proven in the earlier phases.

---

## File Structure (locked in)

### Backend
```
backend/app/ict/                          ← NEW MODULE
├── __init__.py
├── models.py                             ← ICTSession + ICTAnexo
├── service.py                            ← create_session, upload_for_anexo, generate_excel
├── router.py                             ← /api/v1/client/ict/* endpoints
├── schemas.py                            ← Pydantic models
│
├── parsers/                              ← Document extractors
│   ├── __init__.py
│   ├── f101_pdf.py
│   ├── f104_pdf.py
│   ├── f103_pdf.py
│   ├── f108_pdf.py
│   ├── ats_xml.py
│   ├── balance_excel.py
│   ├── mayor_excel.py
│   ├── facturacion_sri.py
│   ├── kardex_excel.py
│   ├── contratos_pdf.py
│   └── certificados_pdf.py
│
├── fillers/                              ← Anexo-specific Excel fillers
│   ├── __init__.py
│   ├── base.py                           ← Filler protocol
│   ├── indice.py
│   ├── a1_mapeo.py
│   ├── a2_ingresos.py
│   ├── a3_costos_gastos.py
│   ├── a4_conciliacion_ingresos.py
│   ├── a5_conciliacion_costos.py
│   ├── a6_beneficios.py
│   ├── a7_credito.py
│   ├── a8_comercio_exterior.py
│   └── a9_inventarios.py
│
├── cell_maps/                            ← Cell → data mapping per anexo
│   ├── __init__.py
│   ├── indice.py
│   ├── a1.py … a9.py
│
├── mapping_catalog.py                    ← F-101 casillero ↔ balance prefixes
└── templates/
    └── ict_2025_template.xlsx            ← Official SRI seed (with formulas)

backend/app/api/__init__.py               ← MODIFY: register ict router
backend/app/db/session.py                 ← MODIFY: register ict models in init_db
```

### Frontend
```
frontend-client/src/ict/                  ← NEW
├── ICTLanding.jsx
├── ICTDashboard.jsx
├── ICTAnexoCard.jsx
├── ICTUploader.jsx
├── ICTEditContribuyenteModal.jsx
└── ictApi.js

frontend-client/src/App.jsx               ← MODIFY: add /tools/ICT_2025 route
frontend-client/src/catalog/ClientCatalog.jsx  ← MODIFY: link ICT_2025 to /tools/ICT_2025
```

### Tests
```
tests/test_ict_models.py
tests/test_ict_service.py
tests/test_ict_router.py
tests/test_ict_parsers_f101.py
tests/test_ict_parsers_balance.py
tests/test_ict_parsers_f104.py
tests/test_ict_parsers_ats.py
tests/test_ict_fillers_indice.py
tests/test_ict_fillers_a1.py
tests/test_ict_fillers_a2.py
tests/test_ict_fillers_a3.py
tests/test_ict_fillers_a4.py
tests/test_ict_fillers_a5.py
tests/test_ict_fillers_a6.py
tests/test_ict_fillers_a7.py
tests/test_ict_fillers_a8.py
tests/test_ict_fillers_a9.py
tests/test_ict_e2e_full_flow.py
tests/fixtures/ict/                       ← Sample F-101, Balance, etc. anonymized
```

### Templates seed
```
backend/app/ict/templates/ict_2025_template.xlsx   ← Copy of official SRI template
```

---

# PHASE 0 — Backend Framework

Goal: DB models, service skeleton, router skeleton, file storage helpers reusable from obligaciones_fiscales.

## Task 1: Create `ict` module skeleton

**Files:**
- Create: `backend/app/ict/__init__.py` (empty)
- Create: `backend/app/ict/parsers/__init__.py` (empty)
- Create: `backend/app/ict/fillers/__init__.py` (empty)
- Create: `backend/app/ict/cell_maps/__init__.py` (empty)
- Create: `backend/app/ict/templates/.gitkeep` (empty)

- [ ] **Step 1: Create directories and empty inits**

```bash
mkdir -p backend/app/ict/parsers backend/app/ict/fillers backend/app/ict/cell_maps backend/app/ict/templates
touch backend/app/ict/__init__.py backend/app/ict/parsers/__init__.py backend/app/ict/fillers/__init__.py backend/app/ict/cell_maps/__init__.py backend/app/ict/templates/.gitkeep
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ict/
git commit -m "feat(ict): scaffold ict module skeleton"
```

---

## Task 2: ICTSession + ICTAnexo models

**Files:**
- Create: `backend/app/ict/models.py`
- Modify: `backend/app/db/session.py` (register new models in init_db)
- Test: `tests/test_ict_models.py`

- [ ] **Step 1: Write the failing test**

Create `tests/test_ict_models.py`:

```python
"""Tests for ICTSession + ICTAnexo models."""
from backend.app.ict.models import ICTSession, ICTAnexo


def test_ict_session_has_expected_columns():
    cols = {c.name for c in ICTSession.__table__.columns}
    expected = {
        "id", "user_id", "ejercicio_fiscal", "ruc", "razon_social",
        "numero_adhesivo", "status", "created_at", "last_activity_at",
        "expires_at",
    }
    assert expected.issubset(cols), f"missing: {expected - cols}"


def test_ict_anexo_has_expected_columns():
    cols = {c.name for c in ICTAnexo.__table__.columns}
    expected = {
        "id", "session_id", "anexo_code", "status",
        "extracted_data", "warnings", "uploaded_files", "last_updated_at",
    }
    assert expected.issubset(cols), f"missing: {expected - cols}"


def test_ict_anexo_has_unique_session_anexo_constraint():
    constraints = [c.name for c in ICTAnexo.__table__.constraints]
    assert "uq_session_anexo" in constraints
```

- [ ] **Step 2: Run test, expect FAIL**

```bash
pytest tests/test_ict_models.py -v
```
Expected: `ImportError: cannot import name 'ICTSession'`

- [ ] **Step 3: Create `backend/app/ict/models.py`**

```python
"""SQLAlchemy models for ICT 2025 sessions and anexos.

ICTSession: persistent 90-day project per client, holds contribuyente data.
ICTAnexo: per-anexo extracted data (survives 24h file deletion).
"""

import datetime

from sqlalchemy import (
    Boolean,
    DateTime,
    ForeignKey,
    Integer,
    String,
    UniqueConstraint,
)
from sqlalchemy.orm import Mapped, mapped_column, relationship
from sqlalchemy.types import JSON

from backend.app.db.session import Base


class ICTSession(Base):
    """Persistent ICT project. 90 days inactivity → expired."""

    __tablename__ = "ict_sessions"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    user_id: Mapped[int] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"), index=True, nullable=False
    )
    ejercicio_fiscal: Mapped[str] = mapped_column(String(4), nullable=False)
    ruc: Mapped[str] = mapped_column(String(20), nullable=False)
    razon_social: Mapped[str] = mapped_column(String(255), nullable=False)
    numero_adhesivo: Mapped[str | None] = mapped_column(String(64), nullable=True)
    status: Mapped[str] = mapped_column(
        String(20), default="in_progress", nullable=False
    )  # in_progress | completed | expired
    created_at: Mapped[datetime.datetime] = mapped_column(
        DateTime, default=datetime.datetime.utcnow, nullable=False
    )
    last_activity_at: Mapped[datetime.datetime] = mapped_column(
        DateTime, default=datetime.datetime.utcnow, nullable=False
    )
    expires_at: Mapped[datetime.datetime] = mapped_column(DateTime, nullable=False)

    anexos: Mapped[list["ICTAnexo"]] = relationship(
        back_populates="session", cascade="all, delete-orphan"
    )


class ICTAnexo(Base):
    """Per-anexo extracted data. Persists 90 days with the session."""

    __tablename__ = "ict_anexos"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    session_id: Mapped[int] = mapped_column(
        ForeignKey("ict_sessions.id", ondelete="CASCADE"), index=True, nullable=False
    )
    anexo_code: Mapped[str] = mapped_column(String(16), nullable=False)
    # INDICE | A1 | A2 | A3 | A4 | A5 | A6 | A7 | A8 | A9

    status: Mapped[str] = mapped_column(String(20), default="empty", nullable=False)
    # empty | partial | ready | error

    extracted_data: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    warnings: Mapped[list | None] = mapped_column(JSON, nullable=True)
    uploaded_files: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    last_updated_at: Mapped[datetime.datetime] = mapped_column(
        DateTime, default=datetime.datetime.utcnow, nullable=False
    )

    session: Mapped["ICTSession"] = relationship(back_populates="anexos")

    __table_args__ = (
        UniqueConstraint("session_id", "anexo_code", name="uq_session_anexo"),
    )
```

- [ ] **Step 4: Register models in `backend/app/db/session.py::init_db`**

Find the imports block at the top of `init_db()` (around line 51) and add:

```python
from backend.app.ict import models as _ict_models  # noqa: F401
```

- [ ] **Step 5: Run test, expect PASS**

```bash
pytest tests/test_ict_models.py -v
```
Expected: 3 PASSED

- [ ] **Step 6: Commit**

```bash
git add backend/app/ict/models.py backend/app/db/session.py tests/test_ict_models.py
git commit -m "feat(ict): ICTSession + ICTAnexo models with 90-day persistence"
```

---

## Task 3: ICT schemas (Pydantic)

**Files:**
- Create: `backend/app/ict/schemas.py`

- [ ] **Step 1: Write schemas**

```python
"""Pydantic schemas for ICT 2025 endpoints."""

import datetime

from pydantic import BaseModel, Field


class CreateSessionRequest(BaseModel):
    ejercicio_fiscal: str = Field(min_length=4, max_length=4, pattern=r"^\d{4}$")
    ruc: str = Field(min_length=10, max_length=13, pattern=r"^\d+$")
    razon_social: str = Field(min_length=1, max_length=255)
    numero_adhesivo: str | None = Field(default=None, max_length=64)


class UpdateSessionRequest(BaseModel):
    ruc: str | None = Field(default=None, min_length=10, max_length=13, pattern=r"^\d+$")
    razon_social: str | None = Field(default=None, min_length=1, max_length=255)
    numero_adhesivo: str | None = Field(default=None, max_length=64)


class AnexoOut(BaseModel):
    anexo_code: str
    status: str
    warnings: list[str] = []
    uploaded_files: dict = {}
    last_updated_at: datetime.datetime


class SessionOut(BaseModel):
    id: int
    ejercicio_fiscal: str
    ruc: str
    razon_social: str
    numero_adhesivo: str | None
    status: str
    created_at: datetime.datetime
    last_activity_at: datetime.datetime
    expires_at: datetime.datetime
    anexos: list[AnexoOut] = []


class UploadResponse(BaseModel):
    anexo_code: str
    status: str
    warnings: list[str] = []
    filename: str
    size_bytes: int
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ict/schemas.py
git commit -m "feat(ict): Pydantic schemas for session + anexo endpoints"
```

---

## Task 4: ICT service — create + get session

**Files:**
- Create: `backend/app/ict/service.py`
- Test: `tests/test_ict_service.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for ICT service layer."""
import datetime
import uuid

import pytest

from backend.app.auth.models import Role
from backend.app.auth.service import create_user, get_user_by_email
from backend.app.context.models import Organization, Client
from backend.app.client_portal.service import create_portal_user
from backend.app.db.session import SessionLocal
from backend.app.ict import service as ict_service


def _unique(prefix: str) -> str:
    return f"{prefix}-{uuid.uuid4().hex[:8]}"


@pytest.fixture()
def db_session():
    db = SessionLocal()
    yield db
    db.close()


@pytest.fixture()
def client_user(db_session):
    suffix = _unique("ict")
    org = Organization(name=f"O-{suffix}", slug=f"o-{suffix}", is_active=True)
    db_session.add(org); db_session.commit(); db_session.refresh(org)
    cli = Client(organization_id=org.id, name=f"C-{suffix}", is_active=True)
    db_session.add(cli); db_session.commit(); db_session.refresh(cli)
    email = f"ictuser-{suffix}@x.com"
    user, _ = create_portal_user(db_session, client_id=cli.id, email=email)
    return user


def test_create_session_sets_expires_at_90_days(db_session, client_user):
    s = ict_service.create_session(
        db_session,
        user=client_user,
        ejercicio_fiscal="2025",
        ruc="1234567890001",
        razon_social="Test S.A.",
        numero_adhesivo=None,
    )
    assert s.status == "in_progress"
    delta = s.expires_at - s.created_at
    assert 89 <= delta.days <= 91


def test_create_session_returns_existing_if_in_progress(db_session, client_user):
    s1 = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="X", numero_adhesivo=None,
    )
    s2 = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="9999999999999", razon_social="Different", numero_adhesivo=None,
    )
    assert s1.id == s2.id  # idempotent
    # Existing RUC/razon_social NOT overwritten
    assert s2.ruc == "1234567890001"


def test_get_active_session_returns_none_if_no_session(db_session, client_user):
    assert ict_service.get_active_session(db_session, user=client_user) is None


def test_get_active_session_returns_in_progress(db_session, client_user):
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="X", numero_adhesivo=None,
    )
    found = ict_service.get_active_session(db_session, user=client_user)
    assert found is not None
    assert found.id == s.id
```

- [ ] **Step 2: Run test, expect FAIL**

```bash
pytest tests/test_ict_service.py -v
```
Expected: `ImportError`

- [ ] **Step 3: Create `backend/app/ict/service.py`**

```python
"""Service layer for ICT 2025: sessions, anexos, Excel generation."""

from __future__ import annotations

import datetime

from sqlalchemy import select
from sqlalchemy.orm import Session

from backend.app.auth.models import User
from backend.app.ict.models import ICTAnexo, ICTSession

SESSION_TTL_DAYS = 90
ANEXOS_CATALOG = ["INDICE", "A1", "A2", "A3", "A4", "A5", "A6", "A7", "A8", "A9"]


def _now() -> datetime.datetime:
    return datetime.datetime.utcnow()


def _expires_at(from_dt: datetime.datetime | None = None) -> datetime.datetime:
    base = from_dt or _now()
    return base + datetime.timedelta(days=SESSION_TTL_DAYS)


def create_session(
    db: Session,
    *,
    user: User,
    ejercicio_fiscal: str,
    ruc: str,
    razon_social: str,
    numero_adhesivo: str | None,
) -> ICTSession:
    """Create new ICT session or return existing in_progress (idempotent)."""
    existing = get_active_session(db, user=user)
    if existing is not None:
        return existing

    now = _now()
    session = ICTSession(
        user_id=user.id,
        ejercicio_fiscal=ejercicio_fiscal,
        ruc=ruc,
        razon_social=razon_social,
        numero_adhesivo=numero_adhesivo,
        status="in_progress",
        created_at=now,
        last_activity_at=now,
        expires_at=_expires_at(now),
    )
    db.add(session)
    db.commit()
    db.refresh(session)

    # Bootstrap one ICTAnexo per anexo code in "empty" state
    for anexo_code in ANEXOS_CATALOG:
        db.add(ICTAnexo(
            session_id=session.id,
            anexo_code=anexo_code,
            status="empty",
            extracted_data=None,
            warnings=None,
            uploaded_files=None,
            last_updated_at=now,
        ))
    db.commit()
    db.refresh(session)
    return session


def get_active_session(db: Session, *, user: User) -> ICTSession | None:
    """Return user's in_progress session or None."""
    return db.execute(
        select(ICTSession)
        .where(ICTSession.user_id == user.id, ICTSession.status == "in_progress")
        .order_by(ICTSession.created_at.desc())
    ).scalars().first()


def get_session(db: Session, *, session_id: int, user: User) -> ICTSession:
    """Get session by id, validates ownership. Raises PermissionError if not owner."""
    s = db.get(ICTSession, session_id)
    if s is None or s.user_id != user.id:
        raise PermissionError("Session not found or not owned by user")
    return s


def touch_session(db: Session, *, session: ICTSession) -> None:
    """Update last_activity_at + extend expires_at."""
    now = _now()
    session.last_activity_at = now
    session.expires_at = _expires_at(now)
    db.add(session)
    db.commit()
```

- [ ] **Step 4: Run test, expect PASS**

```bash
pytest tests/test_ict_service.py -v
```
Expected: 4 PASSED

- [ ] **Step 5: Commit**

```bash
git add backend/app/ict/service.py tests/test_ict_service.py
git commit -m "feat(ict): service layer for session create/get/touch"
```

---

## Task 5: ICT service — update + delete session

**Files:**
- Modify: `backend/app/ict/service.py`
- Test: `tests/test_ict_service.py` (extend)

- [ ] **Step 1: Append failing tests**

Append to `tests/test_ict_service.py`:

```python
def test_update_session_updates_fields_and_touches(db_session, client_user):
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="Old", numero_adhesivo=None,
    )
    original_activity = s.last_activity_at
    import time
    time.sleep(0.01)  # ensure newer timestamp

    updated = ict_service.update_session(
        db_session, session=s, razon_social="New", numero_adhesivo="ADH-1"
    )
    assert updated.razon_social == "New"
    assert updated.numero_adhesivo == "ADH-1"
    assert updated.last_activity_at > original_activity


def test_expire_session_marks_status(db_session, client_user):
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="X", numero_adhesivo=None,
    )
    ict_service.expire_session(db_session, session=s)
    db_session.refresh(s)
    assert s.status == "expired"
```

- [ ] **Step 2: Run test, expect FAIL**

```bash
pytest tests/test_ict_service.py -v -k "update_session or expire_session"
```

- [ ] **Step 3: Append to `backend/app/ict/service.py`**

```python
def update_session(
    db: Session,
    *,
    session: ICTSession,
    ruc: str | None = None,
    razon_social: str | None = None,
    numero_adhesivo: str | None = None,
) -> ICTSession:
    """Update session contribuyente data and touch activity."""
    if ruc is not None:
        session.ruc = ruc
    if razon_social is not None:
        session.razon_social = razon_social
    if numero_adhesivo is not None:
        session.numero_adhesivo = numero_adhesivo
    now = _now()
    session.last_activity_at = now
    session.expires_at = _expires_at(now)
    db.add(session)
    db.commit()
    db.refresh(session)
    return session


def expire_session(db: Session, *, session: ICTSession) -> None:
    """Mark session as expired (user-initiated close)."""
    session.status = "expired"
    db.add(session)
    db.commit()
```

- [ ] **Step 4: Run test, expect PASS**

```bash
pytest tests/test_ict_service.py -v
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/ict/service.py tests/test_ict_service.py
git commit -m "feat(ict): update_session + expire_session"
```

---

## Task 6: ICT router — create + get active

**Files:**
- Create: `backend/app/ict/router.py`
- Modify: `backend/app/api/__init__.py` (register router)
- Test: `tests/test_ict_router.py`

- [ ] **Step 1: Write the failing test**

Create `tests/test_ict_router.py`:

```python
"""Tests for /api/v1/client/ict/* endpoints."""
import uuid

import pytest

from backend.app.client_portal.service import create_portal_user
from backend.app.context.models import Organization, Client
from backend.app.db.session import SessionLocal


def _unique(prefix: str) -> str:
    return f"{prefix}-{uuid.uuid4().hex[:8]}"


@pytest.fixture()
def db_session():
    db = SessionLocal()
    yield db
    db.close()


@pytest.fixture()
def logged_client(client, db_session):
    suffix = _unique("ictrouter")
    org = Organization(name=f"O-{suffix}", slug=f"o-{suffix}", is_active=True)
    db_session.add(org); db_session.commit(); db_session.refresh(org)
    cli = Client(organization_id=org.id, name=f"C-{suffix}", is_active=True)
    db_session.add(cli); db_session.commit(); db_session.refresh(cli)
    email = f"ictr-{suffix}@x.com"
    user, pwd = create_portal_user(db_session, client_id=cli.id, email=email)
    r = client.post(
        "/api/v1/client/auth/login",
        data={"username": email, "password": pwd},
    )
    assert r.status_code == 200, r.json()
    return {"user": user, "token": r.json()["access_token"],
            "headers": {"Authorization": f"Bearer {r.json()['access_token']}"}}


def test_create_session_returns_201_with_session(client, logged_client):
    r = client.post(
        "/api/v1/client/ict/sessions",
        json={
            "ejercicio_fiscal": "2025",
            "ruc": "1234567890001",
            "razon_social": "Test S.A.",
        },
        headers=logged_client["headers"],
    )
    assert r.status_code == 201, r.json()
    body = r.json()
    assert body["ejercicio_fiscal"] == "2025"
    assert body["status"] == "in_progress"
    assert len(body["anexos"]) == 10  # INDICE + A1..A9


def test_get_active_session_returns_404_if_no_session(client, logged_client):
    r = client.get("/api/v1/client/ict/sessions/active", headers=logged_client["headers"])
    assert r.status_code == 404


def test_get_active_session_returns_existing(client, logged_client):
    client.post(
        "/api/v1/client/ict/sessions",
        json={"ejercicio_fiscal": "2025", "ruc": "1234567890001", "razon_social": "X"},
        headers=logged_client["headers"],
    )
    r = client.get("/api/v1/client/ict/sessions/active", headers=logged_client["headers"])
    assert r.status_code == 200
    assert r.json()["ejercicio_fiscal"] == "2025"
```

- [ ] **Step 2: Run test, expect FAIL** (404 on POST)

```bash
pytest tests/test_ict_router.py::test_create_session_returns_201_with_session -v
```

- [ ] **Step 3: Create `backend/app/ict/router.py`**

```python
"""HTTP endpoints for /api/v1/client/ict/*."""

from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from backend.app.auth.deps import require_client_with_device
from backend.app.auth.models import User
from backend.app.db.session import get_db
from backend.app.ict import service as ict_service
from backend.app.ict.schemas import (
    AnexoOut,
    CreateSessionRequest,
    SessionOut,
    UpdateSessionRequest,
)

router = APIRouter(prefix="/client/ict", tags=["client-ict"])


def _session_to_out(session) -> SessionOut:
    return SessionOut(
        id=session.id,
        ejercicio_fiscal=session.ejercicio_fiscal,
        ruc=session.ruc,
        razon_social=session.razon_social,
        numero_adhesivo=session.numero_adhesivo,
        status=session.status,
        created_at=session.created_at,
        last_activity_at=session.last_activity_at,
        expires_at=session.expires_at,
        anexos=[
            AnexoOut(
                anexo_code=a.anexo_code,
                status=a.status,
                warnings=a.warnings or [],
                uploaded_files=a.uploaded_files or {},
                last_updated_at=a.last_updated_at,
            )
            for a in session.anexos
        ],
    )


@router.post(
    "/sessions",
    response_model=SessionOut,
    status_code=status.HTTP_201_CREATED,
)
def create_session_endpoint(
    payload: CreateSessionRequest,
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    session = ict_service.create_session(
        db,
        user=user,
        ejercicio_fiscal=payload.ejercicio_fiscal,
        ruc=payload.ruc,
        razon_social=payload.razon_social,
        numero_adhesivo=payload.numero_adhesivo,
    )
    return _session_to_out(session)


@router.get("/sessions/active", response_model=SessionOut)
def get_active_session_endpoint(
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    session = ict_service.get_active_session(db, user=user)
    if session is None:
        raise HTTPException(404, detail="No hay sesión activa")
    return _session_to_out(session)


@router.get("/sessions/{session_id}", response_model=SessionOut)
def get_session_endpoint(
    session_id: int,
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    try:
        session = ict_service.get_session(db, session_id=session_id, user=user)
    except PermissionError as e:
        raise HTTPException(403, detail=str(e))
    return _session_to_out(session)


@router.patch("/sessions/{session_id}", response_model=SessionOut)
def update_session_endpoint(
    session_id: int,
    payload: UpdateSessionRequest,
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    try:
        session = ict_service.get_session(db, session_id=session_id, user=user)
    except PermissionError as e:
        raise HTTPException(403, detail=str(e))
    updated = ict_service.update_session(
        db, session=session,
        ruc=payload.ruc, razon_social=payload.razon_social,
        numero_adhesivo=payload.numero_adhesivo,
    )
    return _session_to_out(updated)


@router.delete("/sessions/{session_id}", status_code=200)
def delete_session_endpoint(
    session_id: int,
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    try:
        session = ict_service.get_session(db, session_id=session_id, user=user)
    except PermissionError as e:
        raise HTTPException(403, detail=str(e))
    ict_service.expire_session(db, session=session)
    return {"ok": True}
```

- [ ] **Step 4: Register router in `backend/app/api/__init__.py`**

Inspect the file and find where other routers are registered. Add following the existing pattern:

```python
from backend.app.ict.router import router as ict_router
api_router.include_router(ict_router)
```

- [ ] **Step 5: Run tests, expect PASS**

```bash
pytest tests/test_ict_router.py -v
```

- [ ] **Step 6: Commit**

```bash
git add backend/app/ict/router.py backend/app/api/__init__.py tests/test_ict_router.py
git commit -m "feat(ict): session CRUD endpoints (POST, GET active, GET by id, PATCH, DELETE)"
```

---

## Task 7: File storage for ICT (reuse OF helpers)

**Files:**
- Modify: `backend/app/ict/service.py` (add upload helper)
- Test: `tests/test_ict_service.py` (extend)

- [ ] **Step 1: Append test**

```python
def test_save_uploaded_file_writes_to_tmp(db_session, client_user, tmp_path, monkeypatch):
    from backend.app.core.config import settings
    monkeypatch.setattr(settings, "aud_of_tmp_dir_path", tmp_path, raising=False)
    # Reuse OF tmp dir helper
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="X", numero_adhesivo=None,
    )
    saved_path = ict_service.save_uploaded_file(
        session_id=s.id, anexo_code="A1", slot_name="f101",
        filename="test.pdf", data=b"%PDF-1.4 fake",
    )
    assert saved_path.exists()
    assert saved_path.read_bytes() == b"%PDF-1.4 fake"
```

- [ ] **Step 2: Append to `backend/app/ict/service.py`**

```python
from pathlib import Path
from backend.app.aud.obligaciones_fiscales import file_storage


def _ict_job_dir(session_id: int, anexo_code: str) -> Path:
    """Returns the /tmp dir for an ICT anexo (under OF's tmp root for cleanup reuse)."""
    # Use OF's helper which respects AUD_OF_TMP_DIR_PATH; namespace under "ict/"
    of_dir = file_storage._root() / "ict" / f"{session_id}" / anexo_code
    of_dir.mkdir(parents=True, exist_ok=True)
    return of_dir


def save_uploaded_file(
    *,
    session_id: int,
    anexo_code: str,
    slot_name: str,
    filename: str,
    data: bytes,
) -> Path:
    """Persist a raw uploaded file under /tmp/ict/<session>/<anexo>/<slot>/."""
    import re
    safe_filename = re.sub(r"[^a-zA-Z0-9._-]", "_", filename)[:200] or "file"
    safe_slot = re.sub(r"[^a-zA-Z0-9._-]", "_", slot_name)[:64] or "slot"
    slot_dir = _ict_job_dir(session_id, anexo_code) / safe_slot
    slot_dir.mkdir(parents=True, exist_ok=True)
    target = slot_dir / safe_filename
    target.write_bytes(data)
    return target
```

- [ ] **Step 3: Run test + commit**

```bash
pytest tests/test_ict_service.py -v
git add backend/app/ict/service.py tests/test_ict_service.py
git commit -m "feat(ict): save_uploaded_file helper using /tmp/ict/ path"
```

---

## Task 8: Update anexo extracted_data + status

**Files:**
- Modify: `backend/app/ict/service.py`
- Test: `tests/test_ict_service.py`

- [ ] **Step 1: Append test**

```python
def test_update_anexo_data_persists(db_session, client_user):
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="X", numero_adhesivo=None,
    )
    ict_service.update_anexo_data(
        db_session, session=s, anexo_code="A1",
        extracted_data={"f101": {"311": 5000}, "balance": {"1.1.1.01.01": {"nombre": "Caja", "saldo": 5000}}},
        warnings=["Casillero 312 no detectado"],
        uploaded_file_meta={"slot": "f101", "filename": "test.pdf", "size": 1024},
        new_status="partial",
    )
    db_session.refresh(s)
    a1 = next(a for a in s.anexos if a.anexo_code == "A1")
    assert a1.status == "partial"
    assert a1.extracted_data["f101"]["311"] == 5000
    assert "Casillero 312 no detectado" in a1.warnings
    assert a1.uploaded_files["f101"]["filename"] == "test.pdf"
```

- [ ] **Step 2: Append to `service.py`**

```python
def update_anexo_data(
    db: Session,
    *,
    session: ICTSession,
    anexo_code: str,
    extracted_data: dict,
    warnings: list[str],
    uploaded_file_meta: dict,
    new_status: str,
) -> ICTAnexo:
    """Merge extracted data into the anexo, append warnings, set status.

    extracted_data is MERGED (top-level keys overwrite). warnings is APPENDED.
    uploaded_file_meta is keyed by slot_name and merged into uploaded_files.
    """
    anexo = next((a for a in session.anexos if a.anexo_code == anexo_code), None)
    if anexo is None:
        raise ValueError(f"Anexo {anexo_code} not in session")

    existing_data = anexo.extracted_data or {}
    merged = {**existing_data, **extracted_data}
    anexo.extracted_data = merged

    existing_warnings = anexo.warnings or []
    anexo.warnings = existing_warnings + (warnings or [])

    existing_files = anexo.uploaded_files or {}
    slot = uploaded_file_meta.get("slot")
    if slot:
        existing_files[slot] = uploaded_file_meta
    anexo.uploaded_files = existing_files

    anexo.status = new_status
    anexo.last_updated_at = _now()

    # Touch session
    touch_session(db, session=session)

    db.add(anexo)
    db.commit()
    db.refresh(anexo)
    return anexo
```

- [ ] **Step 3: Run + commit**

```bash
pytest tests/test_ict_service.py::test_update_anexo_data_persists -v
git add backend/app/ict/service.py tests/test_ict_service.py
git commit -m "feat(ict): update_anexo_data merges extracted_data, warnings, file meta"
```

---

## Task 9: Bootstrap empty INDICE anexo logic

The `INDICE` anexo is special: it has NO file inputs; its state derives from the other anexos' statuses. We bootstrap it with the cliente data on session create, and we recompute it whenever any other anexo changes.

**Files:**
- Modify: `backend/app/ict/service.py`
- Test: `tests/test_ict_service.py`

- [ ] **Step 1: Append test**

```python
def test_indice_recomputed_after_anexo_updated(db_session, client_user):
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="X", numero_adhesivo="ABC-1",
    )
    # Update A1 to ready
    ict_service.update_anexo_data(
        db_session, session=s, anexo_code="A1",
        extracted_data={"f101": {"311": 100}},
        warnings=[], uploaded_file_meta={"slot": "f101"},
        new_status="ready",
    )
    ict_service.recompute_indice(db_session, session=s)
    db_session.refresh(s)
    indice = next(a for a in s.anexos if a.anexo_code == "INDICE")
    assert indice.status == "ready"
    assert indice.extracted_data["aplica"]["A1"] == "SI"
    assert indice.extracted_data["aplica"]["A2"] == "NO"  # still empty
```

- [ ] **Step 2: Append to `service.py`**

```python
def recompute_indice(db: Session, *, session: ICTSession) -> ICTAnexo:
    """Rebuild INDICE.extracted_data['aplica'] from other anexos' statuses.

    Rule: SI if anexo.status in ('partial', 'ready'); NO otherwise.
    INDICE itself is always 'ready' after recompute (it has no inputs).
    """
    aplica: dict[str, str] = {}
    for a in session.anexos:
        if a.anexo_code == "INDICE":
            continue
        aplica[a.anexo_code] = "SI" if a.status in ("partial", "ready") else "NO"

    indice = next(a for a in session.anexos if a.anexo_code == "INDICE")
    indice.extracted_data = {"aplica": aplica}
    indice.status = "ready"
    indice.last_updated_at = _now()
    db.add(indice)
    db.commit()
    db.refresh(indice)
    return indice
```

- [ ] **Step 3: Run + commit**

```bash
pytest tests/test_ict_service.py::test_indice_recomputed_after_anexo_updated -v
git add backend/app/ict/service.py tests/test_ict_service.py
git commit -m "feat(ict): recompute_indice rebuilds aplica map from anexo statuses"
```

---

## Task 10: Cleanup integration for ICT files

**Files:**
- Modify: `backend/app/aud/obligaciones_fiscales/cleanup.py` (extend orphan detection)

- [ ] **Step 1: Verify current cleanup**

```bash
grep -n "list_orphan_job_dirs" backend/app/aud/obligaciones_fiscales/file_storage.py
```

The existing cleanup detects orphan dirs in OF tmp root. ICT files live under `<root>/ict/<session>/<anexo>/<slot>/<file>`. Need to extend.

- [ ] **Step 2: Add ICT cleanup helper to `backend/app/ict/service.py`**

```python
def cleanup_ict_orphan_files(max_age_hours: int = 24) -> int:
    """Delete ICT files older than max_age_hours.
    The extracted_data in DB is preserved (only the raw files go).
    """
    import shutil
    import time
    from backend.app.aud.obligaciones_fiscales import file_storage as of_storage

    root = of_storage._root() / "ict"
    if not root.exists():
        return 0
    cutoff = time.time() - (max_age_hours * 3600)
    deleted = 0
    for session_dir in root.iterdir():
        if not session_dir.is_dir():
            continue
        for anexo_dir in session_dir.iterdir():
            if not anexo_dir.is_dir():
                continue
            for slot_dir in anexo_dir.iterdir():
                if not slot_dir.is_dir():
                    continue
                for f in slot_dir.iterdir():
                    if f.is_file() and f.stat().st_mtime < cutoff:
                        try:
                            f.unlink()
                            deleted += 1
                        except OSError:
                            pass
    return deleted
```

- [ ] **Step 3: Wire into the existing cleanup loop**

In `backend/app/aud/obligaciones_fiscales/cleanup.py`, modify `cleanup_once()` to also call `cleanup_ict_orphan_files()`:

```python
# Add to imports at top:
from backend.app.ict import service as ict_service

# In cleanup_once(), after the existing OF cleanup blocks, add:
summary["ict_files_deleted"] = ict_service.cleanup_ict_orphan_files(max_age_hours=24)
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/ict/service.py backend/app/aud/obligaciones_fiscales/cleanup.py
git commit -m "feat(ict): cleanup deletes ICT raw files >24h, preserves DB extracted_data"
```

---

# PHASE 1 — Common Parsers

Goal: All document extractors (PDFs, Excels, XML). Each returns a normalized dict that the fillers consume.

## Task 11: F-101 PDF parser

**Files:**
- Create: `backend/app/ict/parsers/f101_pdf.py`
- Test: `tests/test_ict_parsers_f101.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for F-101 PDF parser."""
from pathlib import Path

import pytest

from backend.app.ict.parsers.f101_pdf import parse_f101, ALL_F101_CASILLEROS


def test_all_f101_casilleros_includes_required_for_a1_a9():
    # A1 needs casilleros 311, 315, 325, 336, 337, 339, 342, 343, etc.
    required_a1 = {"311", "315", "325", "336", "337", "339", "342", "343", "358", "361", "362", "499", "513", "534", "699"}
    # A9 needs casilleros 7001, 7010, 7013, 7022, 7025, 7028, 7031, 7034, 7037
    required_a9 = {"7001", "7010", "7013", "7022", "7025", "7028", "7031", "7034", "7037"}
    # A3 needs 6999, 7185, 7186, 7992, etc.
    required_a3 = {"6999", "7185", "7186", "7992", "7182", "7183", "7173", "7174"}

    assert required_a1.issubset(set(ALL_F101_CASILLEROS))
    assert required_a9.issubset(set(ALL_F101_CASILLEROS))
    assert required_a3.issubset(set(ALL_F101_CASILLEROS))


def test_parse_f101_returns_empty_for_invalid_pdf():
    result = parse_f101(b"not a pdf")
    assert result["casilleros"] == {}
    assert "errores" in result
    assert len(result["errores"]) > 0


def test_parse_f101_handles_empty_text():
    # A truly empty PDF would be just header bytes
    empty_pdf = b"%PDF-1.4\n%\xe2\xe3\xcf\xd3\n1 0 obj\n<<>>\nendobj\nxref\n0 1\ntrailer<<>>\nstartxref\n0\n%%EOF\n"
    result = parse_f101(empty_pdf)
    # Should not crash; either error or empty casilleros
    assert "casilleros" in result
    assert "errores" in result
```

- [ ] **Step 2: Run test, expect FAIL**

```bash
pytest tests/test_ict_parsers_f101.py -v
```

- [ ] **Step 3: Create `backend/app/ict/parsers/f101_pdf.py`**

```python
"""Parser for SRI Formulario 101 (Declaración Renta Sociedades) PDF.

Reuses regex helpers from obligaciones_fiscales for casillero extraction.
"""

from __future__ import annotations

from io import BytesIO

import pdfplumber

from backend.app.aud.obligaciones_fiscales.cedulas.base import (
    find_casillero_value,
    find_periodo,
)

# Casilleros relevantes para todos los anexos del MVP.
# Lista combinada: A1 (Estado Situación Financiera, Estado Resultados),
# A3 (límites de deducibilidad), A4 (exentos), A5 (no deducibles),
# A6 (beneficios), A7 (crédito), A9 (inventarios).
ALL_F101_CASILLEROS: list[str] = [
    # Estado Situación Financiera (Activos)
    "311", "312", "313", "314", "315", "316", "317", "318", "319", "320",
    "321", "322", "323", "324", "325", "326", "327", "328", "329", "330",
    "336", "337", "338", "339", "340", "341", "342", "343", "344", "345",
    "346", "347", "358", "359", "360", "361",
    # Activos no corrientes
    "362", "363", "364", "365", "366", "367", "368", "369", "370", "371",
    "372", "373", "374", "375", "376", "377", "378", "379", "380", "381",
    "382", "383", "384", "385", "386", "387", "388", "389", "390",
    "432", "433", "434", "435", "440", "449", "499",
    # Pasivos corrientes
    "513", "514", "515", "516", "517", "518", "519", "520", "521", "522",
    "523", "524", "525", "526", "527", "528", "529", "530", "531", "532",
    "533", "534", "535", "536", "537", "538", "539", "540", "541", "542",
    "543", "544", "545", "546", "547", "548", "549", "569",
    # Patrimonio
    "601", "602", "603", "604", "605", "606", "607", "608", "609", "610",
    "611", "612", "613", "614", "615", "616", "617", "618", "619", "620",
    "699",
    # Conciliación tributaria
    "801", "802", "803", "804", "805", "806", "807", "808", "809", "810",
    "811", "812", "813", "814", "815", "816", "817", "818", "819", "820",
    "828", "829", "834", "835",
    # Diferencias temporarias
    "1112", "1113", "1116", "1117",
    # Ingresos
    "6001", "6005", "6011", "6015", "6021", "6025", "6031", "6035", "6041",
    "6045", "6051", "6055", "6061", "6065", "6071", "6075", "6081", "6085",
    "6091", "6095", "6999",
    # Costos y gastos (con foco en límites de A3)
    "7001", "7010", "7013", "7022", "7025", "7028", "7031", "7034", "7037",
    "7113", "7114", "7173", "7174", "7182", "7183", "7185", "7186",
    "7205", "7206", "7207", "7223", "7224", "7225", "7226", "7227", "7228",
    "7235", "7236", "7237",
    "7278", "7279", "7290", "7291",
    "7992", "7999",
]


def parse_f101(pdf_bytes: bytes) -> dict:
    """Read F-101 PDF and return {'periodo': 'YYYY' | None, 'casilleros': {num: float}, 'errores': []}.

    Devuelve siempre estas 3 claves aunque el PDF sea inválido (errores poblado).
    """
    try:
        with pdfplumber.open(BytesIO(pdf_bytes)) as pdf:
            text = "\n".join(p.extract_text() or "" for p in pdf.pages)
    except Exception as e:  # noqa: BLE001
        return {"periodo": None, "casilleros": {}, "errores": [f"PDF inválido: {e}"]}

    if not text.strip():
        return {
            "periodo": None,
            "casilleros": {},
            "errores": ["PDF sin texto extraíble (¿escaneado? Aplica OCR e intenta de nuevo)"],
        }

    periodo = find_periodo(text)
    casilleros: dict[str, float] = {}
    for num in ALL_F101_CASILLEROS:
        v = find_casillero_value(text, num)
        if v is not None:
            casilleros[num] = v

    return {"periodo": periodo, "casilleros": casilleros, "errores": []}
```

- [ ] **Step 4: Run + commit**

```bash
pytest tests/test_ict_parsers_f101.py -v
git add backend/app/ict/parsers/f101_pdf.py tests/test_ict_parsers_f101.py
git commit -m "feat(ict/parsers): F-101 PDF extractor with ~250 casilleros"
```

---

## Task 12: Balance de Comprobación Excel parser

**Files:**
- Create: `backend/app/ict/parsers/balance_excel.py`
- Test: `tests/test_ict_parsers_balance.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for Balance Excel parser."""
import io
import openpyxl

from backend.app.ict.parsers.balance_excel import parse_balance


def _make_balance_excel(rows: list[tuple]) -> bytes:
    """Helper: builds a minimal balance Excel with header + given rows."""
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.append(["Código", "Nombre", "Saldo Final"])
    for r in rows:
        ws.append(r)
    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)
    return buf.read()


def test_parse_balance_extracts_accounts():
    data = _make_balance_excel([
        ("1.1.01.01.01", "CAJA CHICA UIO", 300.00),
        ("1.1.01.02.01", "BANCO PICHINCHA", 280330.14),
        ("1.1.02.05.01", "CLIENTES", 1718019.45),
    ])
    result = parse_balance(data)
    assert result["errores"] == []
    cuentas = result["cuentas"]
    assert "1.1.01.01.01" in cuentas
    assert cuentas["1.1.01.01.01"]["nombre"] == "CAJA CHICA UIO"
    assert cuentas["1.1.01.01.01"]["saldo"] == 300.00
    assert cuentas["1.1.02.05.01"]["saldo"] == 1718019.45


def test_parse_balance_skips_empty_codes():
    data = _make_balance_excel([
        ("1.1.01.01.01", "CAJA", 100.0),
        (None, "Sub-total", 100.0),  # empty code → skip
        ("", "Header row", None),    # empty code → skip
    ])
    result = parse_balance(data)
    assert len(result["cuentas"]) == 1


def test_parse_balance_returns_error_for_missing_header():
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.append(["foo", "bar"])  # no recognizable header
    ws.append((1, 2))
    buf = io.BytesIO()
    wb.save(buf)
    result = parse_balance(buf.getvalue())
    assert "errores" in result and len(result["errores"]) > 0
```

- [ ] **Step 2: Run test, expect FAIL**

- [ ] **Step 3: Create `backend/app/ict/parsers/balance_excel.py`**

```python
"""Parser for Balance de Comprobación Excel.

Tolerant detection of header row + column positions.
Expected columns (case-insensitive, multi-name): 'Código', 'Nombre', 'Saldo'.
"""

from __future__ import annotations

from io import BytesIO

from openpyxl import load_workbook

_CODIGO_KEYWORDS = ("codigo", "código", "cuenta", "code")
_NOMBRE_KEYWORDS = ("nombre", "descripcion", "descripción", "name", "concepto")
_SALDO_KEYWORDS = ("saldo final", "saldo", "valor", "monto")


def _norm(text) -> str:
    return str(text or "").strip().lower()


def _find_header_row(ws, max_scan: int = 20) -> tuple[int, dict[str, int]] | tuple[None, None]:
    """Scan top N rows for a header with codigo/nombre/saldo. Returns (row_idx, {kind: col_idx})."""
    for row_idx in range(1, min(max_scan, ws.max_row) + 1):
        row = [_norm(c.value) for c in ws[row_idx]]
        col_map: dict[str, int] = {}
        for col_idx, cell in enumerate(row):
            if any(k in cell for k in _CODIGO_KEYWORDS) and "codigo" not in col_map:
                col_map["codigo"] = col_idx
            elif any(k in cell for k in _NOMBRE_KEYWORDS) and "nombre" not in col_map:
                col_map["nombre"] = col_idx
            elif any(k in cell for k in _SALDO_KEYWORDS) and "saldo" not in col_map:
                col_map["saldo"] = col_idx
        if "codigo" in col_map and "saldo" in col_map:
            return row_idx, col_map
    return None, None


def parse_balance(excel_bytes: bytes) -> dict:
    """Returns {'cuentas': {codigo: {nombre, saldo}}, 'errores': []}."""
    try:
        wb = load_workbook(BytesIO(excel_bytes), data_only=True, read_only=True)
    except Exception as e:  # noqa: BLE001
        return {"cuentas": {}, "errores": [f"Excel inválido: {e}"]}

    ws = wb.active
    header_row, col_map = _find_header_row(ws)
    if header_row is None:
        return {
            "cuentas": {},
            "errores": [
                "No se detectó fila de encabezado con 'Código' y 'Saldo'. "
                "Asegúrate de que tu Excel tenga columnas claramente etiquetadas."
            ],
        }

    cuentas: dict[str, dict] = {}
    errores: list[str] = []
    col_codigo = col_map["codigo"]
    col_nombre = col_map.get("nombre")
    col_saldo = col_map["saldo"]

    for row in ws.iter_rows(min_row=header_row + 1, values_only=True):
        codigo_raw = row[col_codigo] if col_codigo < len(row) else None
        if codigo_raw is None or str(codigo_raw).strip() == "":
            continue
        codigo = str(codigo_raw).strip()
        nombre = (
            str(row[col_nombre]).strip()
            if col_nombre is not None and col_nombre < len(row) and row[col_nombre] is not None
            else ""
        )
        saldo_raw = row[col_saldo] if col_saldo < len(row) else None
        try:
            saldo = float(saldo_raw) if saldo_raw is not None else 0.0
        except (ValueError, TypeError):
            errores.append(f"Saldo inválido para código {codigo}")
            continue
        cuentas[codigo] = {"nombre": nombre, "saldo": saldo}

    return {"cuentas": cuentas, "errores": errores}
```

- [ ] **Step 4: Run + commit**

```bash
pytest tests/test_ict_parsers_balance.py -v
git add backend/app/ict/parsers/balance_excel.py tests/test_ict_parsers_balance.py
git commit -m "feat(ict/parsers): Balance Excel parser with tolerant header detection"
```

---

## Task 13: F-104 PDF parser (reuse OF)

**Files:**
- Create: `backend/app/ict/parsers/f104_pdf.py` (thin wrapper)

- [ ] **Step 1: Create wrapper**

```python
"""F-104 IVA PDF parser. Reuses obligaciones_fiscales extract_f104."""

from __future__ import annotations

from pathlib import Path

from backend.app.aud.obligaciones_fiscales.cedulas.f104_extractor import (
    extract_f104 as _of_extract_f104,
    extract_all_f104 as _of_extract_all_f104,
)


def parse_f104(pdf_bytes: bytes) -> dict | None:
    """Parse single F-104 PDF. Returns {'periodo', 'casilleros'} or None."""
    return _of_extract_f104(pdf_bytes)


def parse_all_f104(paths: list[Path]) -> tuple[dict[str, dict], list[str]]:
    """Parse multiple F-104 PDFs, group by month. Returns (month_data, errors)."""
    return _of_extract_all_f104(paths)
```

- [ ] **Step 2: Commit (no new test; the underlying logic is already tested in OF tests)**

```bash
git add backend/app/ict/parsers/f104_pdf.py
git commit -m "feat(ict/parsers): F-104 wrapper reusing obligaciones_fiscales extractor"
```

---

## Task 14: ATS XML parser (for A8)

**Files:**
- Create: `backend/app/ict/parsers/ats_xml.py`
- Test: `tests/test_ict_parsers_ats.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for ATS XML parser."""
from backend.app.ict.parsers.ats_xml import parse_ats


SAMPLE_ATS = """<?xml version="1.0" encoding="UTF-8"?>
<iva>
  <TipoIDInformante>R</TipoIDInformante>
  <IdInformante>1234567890001</IdInformante>
  <razonSocial>Test S.A.</razonSocial>
  <Anio>2024</Anio>
  <Mes>07</Mes>
  <numEstabRuc>001</numEstabRuc>
  <pagoExterior>
    <pagoLocExt>02</pagoLocExt>
    <tipoRegi>01</tipoRegi>
    <paisEfecPago>USA</paisEfecPago>
    <paisEfecPagoGen>NA</paisEfecPagoGen>
    <denopagoRegFiscal>NA</denopagoRegFiscal>
    <pagExtSujRetNorLeg>SI</pagExtSujRetNorLeg>
    <comPagExtSujRetNorLeg>Pago software</comPagExtSujRetNorLeg>
  </pagoExterior>
</iva>"""


def test_parse_ats_extracts_metadata():
    result = parse_ats(SAMPLE_ATS.encode("utf-8"))
    assert result["errores"] == []
    assert result["ruc_informante"] == "1234567890001"
    assert result["razon_social"] == "Test S.A."
    assert result["periodo"] == "07/2024"


def test_parse_ats_extracts_pagos_exterior():
    result = parse_ats(SAMPLE_ATS.encode("utf-8"))
    pagos = result["pagos_exterior"]
    assert len(pagos) == 1
    assert pagos[0]["pais"] == "USA"
    assert pagos[0]["sujeto_retencion"] == "SI"


def test_parse_ats_returns_error_for_invalid_xml():
    result = parse_ats(b"not xml")
    assert len(result["errores"]) > 0
```

- [ ] **Step 2: Create `backend/app/ict/parsers/ats_xml.py`**

```python
"""Parser for ATS (Anexo Transaccional Simplificado) XML del SRI.

Extrae:
- Metadata (RUC, razón social, período)
- Pagos al exterior (para Anexo 8 del ICT)
- Compras/ventas con foco en transacciones internacionales
"""

from __future__ import annotations

from xml.etree import ElementTree as ET


def parse_ats(xml_bytes: bytes) -> dict:
    """Returns {ruc_informante, razon_social, periodo, pagos_exterior: [...], errores: []}."""
    try:
        root = ET.fromstring(xml_bytes)
    except ET.ParseError as e:
        return {
            "ruc_informante": None, "razon_social": None, "periodo": None,
            "pagos_exterior": [], "errores": [f"XML inválido: {e}"],
        }

    def _text(parent, tag: str) -> str | None:
        el = parent.find(tag)
        return el.text.strip() if el is not None and el.text else None

    ruc = _text(root, "IdInformante")
    razon = _text(root, "razonSocial")
    anio = _text(root, "Anio")
    mes = _text(root, "Mes")
    periodo = f"{mes}/{anio}" if anio and mes else None

    pagos_exterior: list[dict] = []
    for pe in root.findall("pagoExterior"):
        pagos_exterior.append({
            "pago_loc_ext": _text(pe, "pagoLocExt"),
            "tipo_regi": _text(pe, "tipoRegi"),
            "pais": _text(pe, "paisEfecPago"),
            "pais_pago_gen": _text(pe, "paisEfecPagoGen"),
            "denop_reg_fiscal": _text(pe, "denopagoRegFiscal"),
            "sujeto_retencion": _text(pe, "pagExtSujRetNorLeg"),
            "comentario": _text(pe, "comPagExtSujRetNorLeg"),
        })

    return {
        "ruc_informante": ruc,
        "razon_social": razon,
        "periodo": periodo,
        "pagos_exterior": pagos_exterior,
        "errores": [],
    }
```

- [ ] **Step 3: Run + commit**

```bash
pytest tests/test_ict_parsers_ats.py -v
git add backend/app/ict/parsers/ats_xml.py tests/test_ict_parsers_ats.py
git commit -m "feat(ict/parsers): ATS XML parser for A8 (pagos al exterior)"
```

---

## Task 15: Mayor Excel parser (cuentas analíticas para A4, A5)

**Files:**
- Create: `backend/app/ict/parsers/mayor_excel.py`

- [ ] **Step 1: Create parser**

```python
"""Parser para Libro Mayor analítico Excel.

Similar al Balance pero con detalle de movimientos.
Para A4 (cuentas de ingresos exentos) y A5 (cuentas de gastos no deducibles).
"""

from __future__ import annotations

from io import BytesIO

from openpyxl import load_workbook


def parse_mayor(excel_bytes: bytes, cuentas_filtro: list[str] | None = None) -> dict:
    """Parse libro mayor. Optionally filter by account code prefixes.

    Returns {movimientos: [{codigo, nombre, debe, haber, saldo, tipo}], errores: []}.
    """
    try:
        wb = load_workbook(BytesIO(excel_bytes), data_only=True, read_only=True)
    except Exception as e:  # noqa: BLE001
        return {"movimientos": [], "errores": [f"Excel inválido: {e}"]}

    ws = wb.active
    # Reuse the balance header detection
    from backend.app.ict.parsers.balance_excel import _find_header_row, _norm
    header_row, col_map = _find_header_row(ws)
    if header_row is None:
        return {"movimientos": [], "errores": ["Encabezado no detectado"]}

    # Look for additional columns: debe, haber, tipo
    header = [_norm(c.value) for c in ws[header_row]]
    extra: dict[str, int] = {}
    for i, h in enumerate(header):
        if "debe" in h and "debe" not in extra:
            extra["debe"] = i
        elif "haber" in h and "haber" not in extra:
            extra["haber"] = i
        elif "tipo" in h and "tipo" not in extra:
            extra["tipo"] = i

    movimientos: list[dict] = []
    col_codigo = col_map["codigo"]
    col_nombre = col_map.get("nombre")
    col_saldo = col_map["saldo"]

    for row in ws.iter_rows(min_row=header_row + 1, values_only=True):
        codigo_raw = row[col_codigo] if col_codigo < len(row) else None
        if codigo_raw is None or str(codigo_raw).strip() == "":
            continue
        codigo = str(codigo_raw).strip()
        if cuentas_filtro and not any(codigo.startswith(p) for p in cuentas_filtro):
            continue
        movimientos.append({
            "codigo": codigo,
            "nombre": str(row[col_nombre]).strip() if col_nombre is not None and row[col_nombre] is not None else "",
            "saldo": float(row[col_saldo]) if row[col_saldo] is not None else 0.0,
            "debe": float(row[extra["debe"]]) if "debe" in extra and row[extra["debe"]] is not None else 0.0,
            "haber": float(row[extra["haber"]]) if "haber" in extra and row[extra["haber"]] is not None else 0.0,
            "tipo": str(row[extra["tipo"]]) if "tipo" in extra and row[extra["tipo"]] is not None else "",
        })

    return {"movimientos": movimientos, "errores": []}
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ict/parsers/mayor_excel.py
git commit -m "feat(ict/parsers): Mayor Excel parser with optional account filter"
```

---

## Task 16: Kardex Excel parser (para A9)

**Files:**
- Create: `backend/app/ict/parsers/kardex_excel.py`

- [ ] **Step 1: Create parser**

```python
"""Parser para Kardex de inventarios Excel.

Lee items con: código de cuenta, descripción, forma valoración,
cantidad existencia, costo total. Soporta múltiples formatos
comunes de ERPs ecuatorianos (SQUAD, Genesys, SAP B1).
"""

from __future__ import annotations

from io import BytesIO

from openpyxl import load_workbook


def parse_kardex(excel_bytes: bytes) -> dict:
    """Returns {items: [{codigo_cuenta, descripcion, forma_valoracion, cantidad, costo_total}], errores}."""
    try:
        wb = load_workbook(BytesIO(excel_bytes), data_only=True, read_only=True)
    except Exception as e:  # noqa: BLE001
        return {"items": [], "errores": [f"Excel inválido: {e}"]}

    ws = wb.active
    from backend.app.ict.parsers.balance_excel import _norm

    # Detect header
    header_row = None
    col_map: dict[str, int] = {}
    for r in range(1, min(20, ws.max_row) + 1):
        row = [_norm(c.value) for c in ws[r]]
        for i, h in enumerate(row):
            if ("codigo" in h or "código" in h) and "codigo_cuenta" not in col_map:
                col_map["codigo_cuenta"] = i
            elif ("descripcion" in h or "descripción" in h or "nombre" in h) and "descripcion" not in col_map:
                col_map["descripcion"] = i
            elif ("forma" in h or "valoracion" in h or "valoración" in h or "metodo" in h or "método" in h) and "forma" not in col_map:
                col_map["forma"] = i
            elif ("cantidad" in h or "existencia" in h or "stock" in h) and "cantidad" not in col_map:
                col_map["cantidad"] = i
            elif ("costo" in h or "valor" in h) and "costo_total" not in col_map:
                col_map["costo_total"] = i
        if "codigo_cuenta" in col_map and "costo_total" in col_map:
            header_row = r
            break

    if header_row is None:
        return {"items": [], "errores": ["No se detectó encabezado del Kardex"]}

    items: list[dict] = []
    for row in ws.iter_rows(min_row=header_row + 1, values_only=True):
        codigo = row[col_map["codigo_cuenta"]] if col_map["codigo_cuenta"] < len(row) else None
        if codigo is None or str(codigo).strip() == "":
            continue
        items.append({
            "codigo_cuenta": str(codigo).strip(),
            "descripcion": str(row[col_map["descripcion"]]).strip() if "descripcion" in col_map and col_map["descripcion"] < len(row) and row[col_map["descripcion"]] is not None else "",
            "forma_valoracion": str(row[col_map["forma"]]).strip() if "forma" in col_map and col_map["forma"] < len(row) and row[col_map["forma"]] is not None else "PROMEDIO",
            "cantidad": str(row[col_map["cantidad"]]).strip() if "cantidad" in col_map and col_map["cantidad"] < len(row) and row[col_map["cantidad"]] is not None else "",
            "costo_total": float(row[col_map["costo_total"]]) if col_map["costo_total"] < len(row) and row[col_map["costo_total"]] is not None else 0.0,
        })

    return {"items": items, "errores": []}
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ict/parsers/kardex_excel.py
git commit -m "feat(ict/parsers): Kardex Excel parser tolerant to ERP formats"
```

---

## Task 17-20: Otros parsers (F-103, F-108, Facturación SRI, Contratos PDF)

> **NOTE — Outline mode**: estos parsers siguen el mismo patrón de F-101 (PDF) y Balance (Excel). Cada uno tiene su test correspondiente y commit independiente.

### Task 17: F-103 PDF parser

**Files:** `backend/app/ict/parsers/f103_pdf.py` + test

**Pattern:** copy F-101 parser structure. Casilleros relevantes para A8 son los de retenciones (302-389 según concepto). Reusar `find_casillero_value`.

### Task 18: F-108 PDF parser (ISD)

**Files:** `backend/app/ict/parsers/f108_pdf.py` + test

**Pattern:** F-108 tiene casilleros propios (501-599 aprox.) para ISD pagado. Mismo enfoque regex + pdfplumber.

### Task 19: Reporte Facturación SRI parser

**Files:** `backend/app/ict/parsers/facturacion_sri.py` + test

**Pattern:** Excel exportado de SRI en Línea con resumen mensual de facturas emitidas y notas de crédito. Detección de columnas tipo balance (mes, total, NC).

### Task 20: Contratos de Inversión PDF parser

**Files:** `backend/app/ict/parsers/contratos_pdf.py` + test

**Pattern:** Texto libre. MVP: extracción básica con pdfplumber + regex para fechas, montos, número resolución. Sin LLM (mantenemos determinismo).

---

# PHASE 2 — Fillers Foundation + ÍNDICE + A9

## Task 21: Filler protocol + template loader

**Files:**
- Create: `backend/app/ict/fillers/base.py`
- Test: `tests/test_ict_fillers_base.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for filler base + template loader."""
from backend.app.ict.fillers.base import load_template, Filler


def test_load_template_returns_workbook_with_all_sheets():
    wb = load_template()
    expected_sheets = {
        "INDICE", "MAPEO DE LA DECLARACIÓN A1", "INGRESOS A2",
        "COSTOS  GASTOS A3", "CONCILIACIÓN INGRESOS A4",
        "CONCILIACIÓN COSTOS Y GASTOS A5", "BENEFICIOS TRIBUTARIOS A6",
        "CRÉDITO TRIBUTARIO A7", "COMERCIO EXTERIOR A8", "INVENTARIOS A9",
    }
    sheets = set(wb.sheetnames)
    assert expected_sheets.issubset(sheets)


def test_load_template_preserves_formulas():
    wb = load_template()
    # Spot check: A3 row 17 (referenced earlier as {3}={1-2}) should be a formula
    ws = wb["COSTOS  GASTOS A3"]
    # Find any cell with a formula string
    found_formula = False
    for row in ws.iter_rows():
        for cell in row:
            if isinstance(cell.value, str) and cell.value.startswith("="):
                found_formula = True
                break
        if found_formula:
            break
    # If template has no formulas yet (clean template), still OK; just verify no exception
```

- [ ] **Step 2: Create `backend/app/ict/fillers/base.py`**

```python
"""Filler protocol + helpers for ICT 2025 Excel template manipulation."""

from __future__ import annotations

from pathlib import Path
from typing import Protocol

from openpyxl import Workbook, load_workbook

TEMPLATE_PATH = Path(__file__).parent.parent / "templates" / "ict_2025_template.xlsx"


def load_template() -> Workbook:
    """Load the official SRI ICT 2025 template preserving formulas."""
    if not TEMPLATE_PATH.exists():
        raise FileNotFoundError(
            f"ICT template not found at {TEMPLATE_PATH}. "
            "Copy the official SRI template here before running ICT generation."
        )
    return load_workbook(TEMPLATE_PATH, data_only=False, keep_links=True)


class Filler(Protocol):
    """Protocol every anexo filler implements."""

    anexo_code: str

    def fill(
        self,
        workbook: Workbook,
        session_data: dict,
        anexo_data: dict,
    ) -> dict:
        """Fill cells in the workbook for this anexo's sheet.

        Args:
            workbook: openpyxl Workbook (template, loaded with formulas preserved)
            session_data: {ruc, razon_social, ejercicio_fiscal, numero_adhesivo}
            anexo_data: ICTAnexo.extracted_data for this anexo

        Returns:
            {filled_cells: int, warnings: list[str]}

        IMPORTANT:
        - Only write cells listed in this anexo's cell_map
        - NEVER overwrite cells with formulas (preserve template)
        """
        ...
```

- [ ] **Step 3: Place template seed**

You need to copy the user's `Anexo ICT ejercicio fiscal 2025 (1).xlsx` into the templates directory:

```bash
cp "C:/Users/jcalu/Downloads/Anexo ICT ejercicio fiscal 2025 (1).xlsx" backend/app/ict/templates/ict_2025_template.xlsx
```

- [ ] **Step 4: Run test + commit**

```bash
pytest tests/test_ict_fillers_base.py -v
git add backend/app/ict/fillers/base.py backend/app/ict/templates/ict_2025_template.xlsx tests/test_ict_fillers_base.py
git commit -m "feat(ict/fillers): Filler protocol + template loader (preserves formulas)"
```

---

## Task 22: ÍNDICE cell_map + filler

**Files:**
- Create: `backend/app/ict/cell_maps/indice.py`
- Create: `backend/app/ict/fillers/indice.py`
- Test: `tests/test_ict_fillers_indice.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for ÍNDICE filler."""
from backend.app.ict.fillers.base import load_template
from backend.app.ict.fillers.indice import IndiceFiller


def test_indice_filler_writes_cliente_data():
    wb = load_template()
    filler = IndiceFiller()
    session_data = {
        "razon_social": "Test S.A.",
        "ruc": "1234567890001",
        "ejercicio_fiscal": "2025",
        "numero_adhesivo": "ABC-12345",
    }
    anexo_data = {"aplica": {"A1": "SI", "A2": "NO", "A9": "SI"}}
    result = filler.fill(wb, session_data, anexo_data)
    assert result["filled_cells"] >= 4

    ws = wb["INDICE"]
    # Razón social typically in C3
    assert ws["C3"].value == "Test S.A."
    assert ws["C4"].value == "1234567890001"
```

- [ ] **Step 2: Create `backend/app/ict/cell_maps/indice.py`**

```python
"""Cell map for ÍNDICE sheet of ICT 2025 template."""

INDICE_SHEET = "INDICE"

# Header cells: (cell_address): (source_key_in_session_data)
INDICE_HEADER_MAP = {
    "C3": "razon_social",
    "C4": "ruc",
    "C5": "ejercicio_fiscal",
}

# Anexo rows: row_idx in INDICE sheet → anexo_code
# Column J = "APLICA (SI/NO)"
INDICE_APLICA_MAP = {
    10: "A1",
    12: "A2_C1",
    13: "A2_C2",
    14: "A2_C3",
    16: "A3",
    19: "A4_C1",
    20: "A4_C2",
    22: "A5",
    25: "A6_C1",
    26: "A6_C2",
    27: "A6_C3",
    29: "A7_C1",
    30: "A7_C2",
    32: "A8_C1",
    33: "A8_C2",
    35: "A9",
}

INDICE_APLICA_COLUMN = "J"
```

- [ ] **Step 3: Create `backend/app/ict/fillers/indice.py`**

```python
"""Filler for ÍNDICE sheet."""

from __future__ import annotations

from openpyxl import Workbook

from backend.app.ict.cell_maps.indice import (
    INDICE_APLICA_COLUMN,
    INDICE_APLICA_MAP,
    INDICE_HEADER_MAP,
    INDICE_SHEET,
)


def _anexo_code_to_aplica_key(anexo_code: str) -> str:
    """Maps single anexo code (e.g. 'A2') to its cuadro keys in INDICE (A2_C1, A2_C2, ...).

    For MVP, an anexo is 'aplica=SI' if ANY of its cuadros has status partial/ready.
    """
    return anexo_code


class IndiceFiller:
    anexo_code = "INDICE"

    def fill(
        self,
        workbook: Workbook,
        session_data: dict,
        anexo_data: dict,
    ) -> dict:
        ws = workbook[INDICE_SHEET]
        filled = 0
        warnings: list[str] = []

        # Header (cliente data)
        for cell_addr, key in INDICE_HEADER_MAP.items():
            value = session_data.get(key, "")
            ws[cell_addr] = value
            filled += 1

        # Anexo SI/NO column
        aplica = anexo_data.get("aplica", {})
        for row_idx, cuadro_key in INDICE_APLICA_MAP.items():
            # Determine the base anexo (strip _C suffix if any)
            base_anexo = cuadro_key.split("_C")[0]
            value = aplica.get(base_anexo, "NO")
            ws[f"{INDICE_APLICA_COLUMN}{row_idx}"] = value
            filled += 1

        return {"filled_cells": filled, "warnings": warnings}
```

- [ ] **Step 4: Run + commit**

```bash
pytest tests/test_ict_fillers_indice.py -v
git add backend/app/ict/cell_maps/indice.py backend/app/ict/fillers/indice.py tests/test_ict_fillers_indice.py
git commit -m "feat(ict/fillers): ÍNDICE auto-fill (cliente data + aplica column)"
```

---

## Task 23: A9 cell_map + filler

**Files:**
- Create: `backend/app/ict/cell_maps/a9.py`
- Create: `backend/app/ict/fillers/a9_inventarios.py`
- Test: `tests/test_ict_fillers_a9.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for A9 Inventarios filler."""
from backend.app.ict.fillers.base import load_template
from backend.app.ict.fillers.a9_inventarios import A9Filler


def test_a9_filler_writes_9_casilleros():
    wb = load_template()
    filler = A9Filler()
    session_data = {
        "razon_social": "Test S.A.",
        "ruc": "1234567890001",
        "ejercicio_fiscal": "2025",
        "numero_adhesivo": "ABC-12345",
    }
    anexo_data = {
        "f101": {
            "7001": 3649643.24,
            "7010": 6004284.22,
            "7013": 0.0,
            "7022": 0.0,
            "7025": 0.0,
            "7028": 0.0,
            "7031": 0.0,
            "7034": 0.0,
            "7037": 0.0,
        },
        "kardex_items": [],
    }
    result = filler.fill(wb, session_data, anexo_data)
    assert result["filled_cells"] >= 9

    ws = wb["INVENTARIOS A9"]
    # The 9 casilleros A9 should be filled in their fixed rows (27-35 typically)
    # Verify at least the first value made it
    found_value = False
    for row in ws.iter_rows(min_row=20, max_row=40, values_only=False):
        for cell in row:
            if cell.value == 3649643.24:
                found_value = True
                break
        if found_value:
            break
    assert found_value, "Value for casillero 7001 not found in A9 sheet"
```

- [ ] **Step 2: Create `backend/app/ict/cell_maps/a9.py`**

```python
"""Cell map for INVENTARIOS A9 sheet."""

A9_SHEET = "INVENTARIOS A9"

# Header data (rows 3-6 typically)
A9_HEADER_MAP = {
    "C3": "razon_social",
    "C4": "ruc",
    "C5": "ejercicio_fiscal",
    "C6": "numero_adhesivo",
}

# 9 fixed casilleros in their fixed rows (27-35 based on ARCOLANDS sample)
# {row_idx: casillero_F101}
A9_CASILLEROS = {
    27: "7001",  # INVENTARIO INICIAL DE BIENES NO PRODUCIDOS
    28: "7010",  # INVENTARIO FINAL DE BIENES NO PRODUCIDOS
    29: "7013",  # INVENTARIO INICIAL DE MATERIA PRIMA
    30: "7022",  # INVENTARIO FINAL DE MATERIA PRIMA
    31: "7025",  # INVENTARIO INICIAL DE PRODUCTOS EN PROCESO
    32: "7028",  # INVENTARIO FINAL DE PRODUCTOS EN PROCESO
    33: "7031",  # INVENTARIO INICIAL PRODUCTOS TERMINADOS
    34: "7034",  # INVENTARIO FINAL DE PRODUCTOS TERMINADOS
    35: "7037",  # AJUSTES
}

# Column C = Valor declarado (del F-101)
# Column D = Código de cuenta contable (del kardex)
# Column E = Forma valoración (PROMEDIO, PEPS, IDENTIFICACIÓN ESPECÍFICA)
# Column F = Cantidad existencia
# Column G = Costo total
# Column H = Diferencia (FÓRMULA del template — NO tocar)
```

- [ ] **Step 3: Create `backend/app/ict/fillers/a9_inventarios.py`**

```python
"""Filler for INVENTARIOS A9 sheet."""

from __future__ import annotations

from openpyxl import Workbook

from backend.app.ict.cell_maps.a9 import A9_CASILLEROS, A9_HEADER_MAP, A9_SHEET


class A9Filler:
    anexo_code = "A9"

    def fill(
        self,
        workbook: Workbook,
        session_data: dict,
        anexo_data: dict,
    ) -> dict:
        ws = workbook[A9_SHEET]
        filled = 0
        warnings: list[str] = []

        # Header (cliente data)
        for cell_addr, key in A9_HEADER_MAP.items():
            ws[cell_addr] = session_data.get(key, "")
            filled += 1

        # 9 casilleros F-101 en columna C
        f101 = anexo_data.get("f101", {})
        kardex_items = anexo_data.get("kardex_items", [])
        kardex_by_codigo = {item["codigo_cuenta"]: item for item in kardex_items}

        # Try to match kardex items by account prefix (best-effort)
        # For each fixed row, write F-101 value + try to enrich with kardex
        for row_idx, casillero in A9_CASILLEROS.items():
            valor = f101.get(casillero)
            if valor is not None:
                ws[f"C{row_idx}"] = valor
                filled += 1
            else:
                warnings.append(f"Casillero F-101 {casillero} no detectado")

            # Best-effort kardex matching: first item gets written, others ignored
            if kardex_items:
                # Heuristic: match by typical inventory account prefix
                first_match = next(iter(kardex_items), None)
                if first_match:
                    ws[f"D{row_idx}"] = first_match.get("codigo_cuenta", "")
                    ws[f"E{row_idx}"] = first_match.get("forma_valoracion", "PROMEDIO")
                    ws[f"F{row_idx}"] = first_match.get("cantidad", "")
                    ws[f"G{row_idx}"] = first_match.get("costo_total", 0.0)
                    filled += 4
            elif valor is not None and valor > 0:
                warnings.append(
                    f"Casillero {casillero} tiene valor pero no se subió Kardex — "
                    "celdas D-G quedaron vacías"
                )

        return {"filled_cells": filled, "warnings": warnings}
```

- [ ] **Step 4: Run + commit**

```bash
pytest tests/test_ict_fillers_a9.py -v
git add backend/app/ict/cell_maps/a9.py backend/app/ict/fillers/a9_inventarios.py tests/test_ict_fillers_a9.py
git commit -m "feat(ict/fillers): A9 Inventarios filler (9 casilleros fijos + kardex)"
```

---

## Task 24: generate_excel orchestrator

**Files:**
- Modify: `backend/app/ict/service.py` (add generate_excel)
- Test: `tests/test_ict_service.py` (extend)

- [ ] **Step 1: Append test**

```python
def test_generate_excel_returns_bytes_with_all_sheets(db_session, client_user):
    s = ict_service.create_session(
        db_session, user=client_user, ejercicio_fiscal="2025",
        ruc="1234567890001", razon_social="Test S.A.", numero_adhesivo="ABC-1",
    )
    # Populate A9 with sample data
    ict_service.update_anexo_data(
        db_session, session=s, anexo_code="A9",
        extracted_data={
            "f101": {"7001": 1000.0, "7010": 2000.0, "7013": 0.0, "7022": 0.0,
                     "7025": 0.0, "7028": 0.0, "7031": 0.0, "7034": 0.0, "7037": 0.0},
            "kardex_items": [],
        },
        warnings=[], uploaded_file_meta={"slot": "f101"},
        new_status="ready",
    )
    ict_service.recompute_indice(db_session, session=s)
    db_session.refresh(s)

    excel_bytes = ict_service.generate_excel(db_session, session=s)
    assert isinstance(excel_bytes, bytes)
    assert len(excel_bytes) > 1000  # non-trivial file

    # Open and verify A9 has data
    import io
    import openpyxl
    wb = openpyxl.load_workbook(io.BytesIO(excel_bytes), data_only=False)
    ws = wb["INVENTARIOS A9"]
    # Verify value 1000.0 made it
    found = False
    for row in ws.iter_rows(values_only=True):
        for cell in row:
            if cell == 1000.0:
                found = True
                break
        if found:
            break
    assert found
```

- [ ] **Step 2: Append to `service.py`**

```python
def generate_excel(db: Session, *, session: ICTSession) -> bytes:
    """Generate the ICT Excel by loading template + applying all fillers."""
    from io import BytesIO
    from backend.app.ict.fillers.base import load_template
    from backend.app.ict.fillers.indice import IndiceFiller
    from backend.app.ict.fillers.a9_inventarios import A9Filler

    wb = load_template()
    session_data = {
        "razon_social": session.razon_social,
        "ruc": session.ruc,
        "ejercicio_fiscal": session.ejercicio_fiscal,
        "numero_adhesivo": session.numero_adhesivo or "",
    }

    # Map of anexo_code → filler instance
    filler_map = {
        "INDICE": IndiceFiller(),
        "A9": A9Filler(),
        # A1, A2, A3, A4, A5, A6, A7, A8 added as we build them
    }

    for anexo in session.anexos:
        filler = filler_map.get(anexo.anexo_code)
        if filler is None:
            continue  # Anexo not yet implemented
        if anexo.status in ("empty",):
            continue  # No data to fill
        try:
            filler.fill(wb, session_data, anexo.extracted_data or {})
        except Exception:
            # Log but don't break the whole generation
            import logging
            logging.exception("Filler %s failed for session %s", anexo.anexo_code, session.id)

    buf = BytesIO()
    wb.save(buf)
    buf.seek(0)
    return buf.read()
```

- [ ] **Step 3: Run + commit**

```bash
pytest tests/test_ict_service.py::test_generate_excel_returns_bytes_with_all_sheets -v
git add backend/app/ict/service.py tests/test_ict_service.py
git commit -m "feat(ict): generate_excel orchestrator (loads template + applies fillers)"
```

---

## Task 25: Download endpoint

**Files:**
- Modify: `backend/app/ict/router.py`
- Test: `tests/test_ict_router.py` (extend)

- [ ] **Step 1: Append test**

```python
def test_download_returns_excel_for_existing_session(client, logged_client):
    # Create session
    r = client.post(
        "/api/v1/client/ict/sessions",
        json={"ejercicio_fiscal": "2025", "ruc": "1234567890001", "razon_social": "X"},
        headers=logged_client["headers"],
    )
    session_id = r.json()["id"]

    r2 = client.get(
        f"/api/v1/client/ict/sessions/{session_id}/download",
        headers=logged_client["headers"],
    )
    assert r2.status_code == 200
    assert r2.headers["content-type"] in (
        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        "application/octet-stream",
    )
    assert len(r2.content) > 1000
```

- [ ] **Step 2: Append to `router.py`**

```python
from fastapi.responses import StreamingResponse
from io import BytesIO


@router.get("/sessions/{session_id}/download")
def download_excel_endpoint(
    session_id: int,
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    try:
        session = ict_service.get_session(db, session_id=session_id, user=user)
    except PermissionError as e:
        raise HTTPException(403, detail=str(e))

    excel_bytes = ict_service.generate_excel(db, session=session)
    filename = f"ICT_{session.ejercicio_fiscal}_{session.ruc}.xlsx"
    return StreamingResponse(
        BytesIO(excel_bytes),
        media_type=(
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
        ),
        headers={"Content-Disposition": f'attachment; filename="{filename}"'},
    )
```

- [ ] **Step 3: Run + commit**

```bash
pytest tests/test_ict_router.py::test_download_returns_excel_for_existing_session -v
git add backend/app/ict/router.py tests/test_ict_router.py
git commit -m "feat(ict): GET /sessions/{id}/download streams generated Excel"
```

---

# PHASE 3 — A1 Mapeo (Dynamic Row Insertion)

## Task 26: Mapping catalog F-101 ↔ balance prefixes

**Files:**
- Create: `backend/app/ict/mapping_catalog.py`

- [ ] **Step 1: Create catalog**

```python
"""Maps F-101 casilleros to balance account prefixes.

Derived from ARCOLANDS 2024 + Plan de Cuentas estándar Ecuador (Supercías).
Extensible: future versions can override per cliente via DB.
"""

F101_TO_BALANCE_PREFIXES: dict[str, list[str]] = {
    # === ACTIVOS CORRIENTES ===
    "311": ["1.1.01"],                              # Efectivo y equivalentes
    "312": ["1.1.02.01", "1.1.02.02"],             # Activos financieros a valor razonable
    "313": ["1.1.02.03", "1.1.02.04"],             # Activos financieros disponibles para venta
    "314": ["1.1.02.05"],                          # Activos financieros mantenidos hasta vencimiento
    "315": ["1.1.02.05", "1.1.02.06"],             # Cuentas y documentos por cobrar corrientes
    "325": ["1.1.02.07", "1.1.02.08"],             # Otras cuentas por cobrar
    "336": ["1.1.05.01"],                          # Crédito tributario IVA
    "337": ["1.1.05.02"],                          # Crédito tributario impuesto a la renta
    "339": ["1.1.03.07"],                          # Mercaderías en tránsito
    "342": ["1.1.03.01"],                          # Inventario productos terminados
    "343": ["1.1.03.09"],                          # Inventario suministros y herramientas
    "358": ["1.1.04.01"],                          # Gastos pagados por anticipado - seguros
    "359": ["1.1.04.03"],                          # Gastos pagados por anticipado - otros
    "361": ["1.1"],                                # Total activos corrientes

    # === ACTIVOS NO CORRIENTES ===
    "362": ["1.2.01.01.01"],                       # Terrenos
    "364": ["1.2.01.01.02", "1.2.01.01.03"],       # Edificios y otros inmuebles
    "368": ["1.2.01.01.06"],                       # Maquinaria
    "374": ["1.2.01.01.08"],                       # Equipo de computación
    "384": ["1.2.01.02"],                          # Depreciación acumulada (negativo)
    "432": ["1.2.07.01"],                          # Otras cuentas por cobrar largo plazo
    "440": ["1.2.05.01"],                          # Activos por impuestos diferidos
    "449": ["1.2"],                                # Total activos no corrientes
    "499": ["1."],                                 # Total Activos

    # === PASIVOS CORRIENTES ===
    "513": ["2.1.03.01.01"],                       # Cuentas por pagar proveedores locales
    "514": ["2.1.03.01.02"],                       # Cuentas por pagar proveedores exterior
    "521": ["2.1.09.03"],                          # Otras cuentas por pagar (tarjetas)
    "533": ["2.1.07.05.01"],                       # Participación trabajadores por pagar
    "534": ["2.1.07.03"],                          # Obligaciones con IESS
    "536": ["2.1.07.04"],                          # Otros pasivos corrientes (sueldos, décimos)
    "545": ["2.1.10.01"],                          # Pasivos por ingresos diferidos
    "549": ["2.1.07.01"],                          # Otros pasivos corrientes (retenciones)
    "569": ["2.1"],                                # Total pasivos corrientes

    # === PATRIMONIO ===
    "601": ["3.1.01"],                             # Capital suscrito y/o asignado
    "604": ["3.1.04"],                             # Aportes futuras capitalizaciones
    "609": ["3.3.01"],                             # Reserva legal
    "613": ["3.4.01"],                             # Resultados acumulados (utilidades)
    "615": ["3.4.02"],                             # Resultados del ejercicio
    "699": ["3."],                                 # Total Patrimonio

    # === INGRESOS (Estado Resultados) ===
    "6001": ["4.1.01"],                            # Ventas netas locales gravadas
    "6005": ["4.1.02"],                            # Ventas netas exportaciones
    "6999": ["4."],                                # Total Ingresos

    # === COSTOS Y GASTOS ===
    "7173": ["5.2.01.05"],                         # Gastos promoción y publicidad
    "7182": ["5.2.01.04"],                         # Gastos viaje
    "7185": ["5.2.01.06"],                         # Gastos de gestión
    "7992": ["5."],                                # Total gastos declarados
    "7999": ["5."],                                # Total costos y gastos
}


def get_balance_prefixes_for_casillero(casillero: str) -> list[str]:
    """Returns the balance account prefixes that compose this F-101 casillero."""
    return F101_TO_BALANCE_PREFIXES.get(casillero, [])
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ict/mapping_catalog.py
git commit -m "feat(ict): mapping catalog F-101 casillero ↔ balance account prefixes"
```

---

## Task 27: A1 cell_map

**Files:**
- Create: `backend/app/ict/cell_maps/a1.py`

- [ ] **Step 1: Create cell map**

```python
"""Cell map for MAPEO A1 sheet.

A1 structure (validated with ARCOLANDS):
- Header rows 3-7: cliente data + título
- Data starts row 13
- Each casillero F-101 occupies 1 base row + N subaccount rows
- Columns: A=Casillero, B=Nombre, C=Valor declarado,
           D=Código contable, E=Nombre cuenta, F=Saldo, G=Diferencia (FÓRMULA), H=Obs
"""

A1_SHEET = "MAPEO DE LA DECLARACIÓN A1"
A1_FIRST_DATA_ROW = 13

A1_HEADER_MAP = {
    "C3": "razon_social",
    "C4": "ruc",
    "C5": "ejercicio_fiscal",
    "C6": "numero_adhesivo",
}

# Ordered list of casilleros to fill in A1, with their human-readable names.
# IMPORTANT: order matters because rows are written sequentially.
# Subset of F-101 casilleros relevant to Estado Situación Financiera + Resultados.
A1_CASILLEROS_ORDERED: list[tuple[str, str]] = [
    # Activos corrientes
    ("311", "EFECTIVO Y EQUIVALENTES AL EFECTIVO"),
    ("315", "CUENTAS Y DOCUMENTOS POR COBRAR CORRIENTES"),
    ("325", "OTRAS CUENTAS Y DOCUMENTOS POR COBRAR CORRIENTES"),
    ("336", "ACTIVOS POR IMPUESTOS CORRIENTES / Crédito Tributario IVA"),
    ("337", "ACTIVOS POR IMPUESTOS CORRIENTES / Crédito Tributario Renta"),
    ("339", "INVENTARIOS - Mercaderías en tránsito"),
    ("342", "Inventario de prod. term. y mercad. en almacén"),
    ("343", "Inventario de suministros, herramientas, repuestos"),
    ("358", "GASTOS PAGADOS POR ANTICIPADO - PRIMAS DE SEGURO"),
    ("359", "GASTOS PAGADOS POR ANTICIPADO - OTROS PREPAGADOS"),
    ("361", "TOTAL ACTIVOS CORRIENTES"),
    # Activos no corrientes
    ("362", "TERRENOS"),
    ("364", "EDIFICIOS Y OTROS INMUEBLES"),
    ("368", "MAQUINARIA, EQUIPO, INSTALACIONES Y ADECUACIONES"),
    ("374", "EQUIPO DE COMPUTACIÓN"),
    ("384", "(-) DEPRECIACIÓN ACUMULADA DE PROPIEDADES, PLANTA Y EQUIPO"),
    ("432", "CUENTAS Y DOCUMENTOS POR COBRAR NO CORRIENTES"),
    ("440", "ACTIVOS POR IMPUESTOS DIFERIDOS"),
    ("449", "TOTAL ACTIVOS NO CORRIENTES"),
    ("499", "TOTAL DEL ACTIVO"),
    # Pasivos
    ("513", "CUENTAS Y DOCUMENTOS POR PAGAR CORRIENTES - PROVEEDORES LOCALES"),
    ("514", "CUENTAS Y DOCUMENTOS POR PAGAR CORRIENTES - PROVEEDORES EXTERIOR"),
    ("521", "OTRAS CUENTAS Y DOCUMENTOS POR PAGAR CORRIENTES"),
    ("533", "PARTICIPACIÓN TRABAJADORES POR PAGAR DEL EJERCICIO"),
    ("534", "OBLIGACIONES CON EL IESS"),
    ("536", "OTROS PASIVOS CORRIENTES POR BENEFICIOS A EMPLEADOS"),
    ("545", "PASIVOS POR INGRESOS DIFERIDOS - Anticipos clientes"),
    ("549", "OTROS PASIVOS CORRIENTES"),
    ("569", "TOTAL PASIVOS CORRIENTES"),
    # Patrimonio
    ("601", "CAPITAL SUSCRITO Y/O ASIGNADO"),
    ("604", "APORTES DE SOCIOS PARA FUTURA CAPITALIZACIÓN"),
    ("609", "RESERVA LEGAL"),
    ("613", "RESULTADOS ACUMULADOS / Utilidades retenidas ejercicios anteriores"),
    ("615", "RESULTADO DEL EJERCICIO"),
    ("699", "TOTAL PATRIMONIO"),
    # Ingresos
    ("6001", "VENTAS NETAS LOCALES GRAVADAS"),
    ("6005", "VENTAS NETAS EXPORTACIONES"),
    ("6999", "TOTAL INGRESOS"),
]
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ict/cell_maps/a1.py
git commit -m "feat(ict/cell_maps): A1 casillero ordered list + header map"
```

---

## Task 28: A1 filler with dynamic row insertion

**Files:**
- Create: `backend/app/ict/fillers/a1_mapeo.py`
- Test: `tests/test_ict_fillers_a1.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for A1 Mapeo filler with dynamic row insertion."""
from backend.app.ict.fillers.base import load_template
from backend.app.ict.fillers.a1_mapeo import A1Filler


def test_a1_filler_writes_casillero_311_with_subaccounts():
    wb = load_template()
    filler = A1Filler()
    session_data = {
        "razon_social": "Test S.A.",
        "ruc": "1234567890001",
        "ejercicio_fiscal": "2025",
        "numero_adhesivo": "ABC-1",
    }
    anexo_data = {
        "f101": {"311": 5008023.09},
        "balance": {
            "1.1.01.01.01": {"nombre": "CAJA CHICA UIO", "saldo": 300.00},
            "1.1.01.01.02": {"nombre": "CAJA CHICA SSFD", "saldo": 1000.00},
            "1.1.01.02.01": {"nombre": "BANCO PICHINCHA", "saldo": 280330.14},
        },
    }
    result = filler.fill(wb, session_data, anexo_data)
    assert result["filled_cells"] >= 1 + 3 * 3  # casillero + 3 subaccounts × 3 cols

    ws = wb["MAPEO DE LA DECLARACIÓN A1"]
    # Find the casillero 311 row
    found_311_row = None
    for row_idx in range(13, 30):
        if ws[f"A{row_idx}"].value == "311":
            found_311_row = row_idx
            break
    assert found_311_row is not None, "Casillero 311 not found in A1"
    # Verify the 3 subaccounts are populated in consecutive rows
    assert ws[f"D{found_311_row}"].value == "1.1.01.01.01"
    assert ws[f"F{found_311_row}"].value == 300.00


def test_a1_filler_handles_casillero_with_no_subaccounts():
    """Casillero declared but no balance accounts → just base row written."""
    wb = load_template()
    filler = A1Filler()
    session_data = {"razon_social": "X", "ruc": "1", "ejercicio_fiscal": "2025", "numero_adhesivo": ""}
    anexo_data = {
        "f101": {"311": 1000.0},
        "balance": {},  # no accounts
    }
    result = filler.fill(wb, session_data, anexo_data)
    # Should warn about missing subaccounts
    assert any("subcuentas" in w.lower() or "balance" in w.lower() for w in result["warnings"])
```

- [ ] **Step 2: Create `backend/app/ict/fillers/a1_mapeo.py`**

```python
"""Filler for MAPEO A1 sheet with dynamic row insertion for subaccounts."""

from __future__ import annotations

from openpyxl import Workbook

from backend.app.ict.cell_maps.a1 import (
    A1_CASILLEROS_ORDERED,
    A1_FIRST_DATA_ROW,
    A1_HEADER_MAP,
    A1_SHEET,
)
from backend.app.ict.mapping_catalog import get_balance_prefixes_for_casillero


class A1Filler:
    anexo_code = "A1"

    def fill(
        self,
        workbook: Workbook,
        session_data: dict,
        anexo_data: dict,
    ) -> dict:
        ws = workbook[A1_SHEET]
        filled = 0
        warnings: list[str] = []

        # 1. Header (cliente data)
        for cell_addr, key in A1_HEADER_MAP.items():
            ws[cell_addr] = session_data.get(key, "")
            filled += 1

        f101 = anexo_data.get("f101", {})
        balance = anexo_data.get("balance", {})

        current_row = A1_FIRST_DATA_ROW

        for casillero, casillero_nombre in A1_CASILLEROS_ORDERED:
            valor_declarado = f101.get(casillero)
            prefijos = get_balance_prefixes_for_casillero(casillero)

            # Find balance accounts matching the prefixes
            matching = sorted([
                (codigo, info) for codigo, info in balance.items()
                if any(codigo.startswith(p) for p in prefijos)
            ])

            # Write base row: casillero + nombre + valor declarado
            ws[f"A{current_row}"] = casillero
            ws[f"B{current_row}"] = casillero_nombre
            if valor_declarado is not None:
                ws[f"C{current_row}"] = valor_declarado
                filled += 3
            else:
                filled += 2
                warnings.append(f"Casillero {casillero} no encontrado en F-101")

            if not matching:
                warnings.append(
                    f"Casillero {casillero}: sin subcuentas en balance "
                    f"(prefijos buscados: {prefijos})"
                )
                current_row += 1
                continue

            # First subaccount goes in the base row's D-F
            codigo, info = matching[0]
            ws[f"D{current_row}"] = codigo
            ws[f"E{current_row}"] = info["nombre"]
            ws[f"F{current_row}"] = info["saldo"]
            filled += 3

            # Additional subaccounts require row insertion
            for offset, (codigo, info) in enumerate(matching[1:], start=1):
                ws.insert_rows(current_row + offset)
                ws[f"D{current_row + offset}"] = codigo
                ws[f"E{current_row + offset}"] = info["nombre"]
                ws[f"F{current_row + offset}"] = info["saldo"]
                filled += 3

            current_row += len(matching)

        return {"filled_cells": filled, "warnings": warnings}
```

- [ ] **Step 3: Register A1 in generate_excel orchestrator**

In `backend/app/ict/service.py`, find the `filler_map` in `generate_excel()` and extend:

```python
from backend.app.ict.fillers.a1_mapeo import A1Filler

filler_map = {
    "INDICE": IndiceFiller(),
    "A1": A1Filler(),         # ← add this
    "A9": A9Filler(),
}
```

- [ ] **Step 4: Run + commit**

```bash
pytest tests/test_ict_fillers_a1.py -v
git add backend/app/ict/fillers/a1_mapeo.py backend/app/ict/service.py tests/test_ict_fillers_a1.py
git commit -m "feat(ict/fillers): A1 Mapeo with dynamic row insertion for subaccounts"
```

---

## Task 29: Upload endpoint for A1

**Files:**
- Modify: `backend/app/ict/router.py`
- Test: `tests/test_ict_router.py`

- [ ] **Step 1: Append test**

```python
import io


def test_upload_f101_for_a1_populates_anexo(client, logged_client, db_session):
    # Create session
    r = client.post(
        "/api/v1/client/ict/sessions",
        json={"ejercicio_fiscal": "2025", "ruc": "1234567890001", "razon_social": "X"},
        headers=logged_client["headers"],
    )
    session_id = r.json()["id"]

    # Upload a fake F-101 (PDF that contains a parseable casillero)
    # Use minimal but valid PDF with F-101 text
    fake_f101_text = b"""%PDF-1.4
1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj
2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj
3 0 obj<</Type/Page/Parent 2 0 R/Resources<<>>/MediaBox[0 0 612 792]/Contents 4 0 R>>endobj
4 0 obj<</Length 120>>stream
BT
/F1 12 Tf
100 700 Td
(Periodo Fiscal: 2025) Tj
0 -20 Td
(311 5008023.09) Tj
ET
endstream
endobj
xref
0 5
0000000000 65535 f
trailer<</Size 5/Root 1 0 R>>
startxref
0
%%EOF
"""
    r2 = client.post(
        f"/api/v1/client/ict/sessions/{session_id}/anexos/A1/upload",
        files={"file": ("F-101.pdf", io.BytesIO(fake_f101_text), "application/pdf")},
        data={"slot_name": "f101"},
        headers=logged_client["headers"],
    )
    assert r2.status_code in (200, 201), r2.json()
    body = r2.json()
    assert body["anexo_code"] == "A1"
    # Status may be partial (no balance yet) or error if parse failed
    assert body["status"] in ("partial", "ready", "error")
```

- [ ] **Step 2: Append to `router.py`**

```python
from fastapi import File, Form, UploadFile

from backend.app.ict.parsers.f101_pdf import parse_f101
from backend.app.ict.parsers.balance_excel import parse_balance
from backend.app.ict.parsers.kardex_excel import parse_kardex
from backend.app.ict.schemas import UploadResponse

# Slot → parser fn mapping
SLOT_PARSERS = {
    "f101": parse_f101,
    "balance": parse_balance,
    "kardex": parse_kardex,
    # Extended in later phases for f104, ats, mayor, etc.
}

# Anexo → list of required slots
ANEXO_REQUIRED_SLOTS = {
    "A1": ["f101", "balance"],
    "A9": ["f101"],  # kardex is optional
    # A2, A3, ... added as we build them
}


@router.post(
    "/sessions/{session_id}/anexos/{anexo_code}/upload",
    response_model=UploadResponse,
)
async def upload_for_anexo_endpoint(
    session_id: int,
    anexo_code: str,
    slot_name: str = Form(...),
    file: UploadFile = File(...),
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    try:
        session = ict_service.get_session(db, session_id=session_id, user=user)
    except PermissionError as e:
        raise HTTPException(403, detail=str(e))

    if session.status != "in_progress":
        raise HTTPException(410, detail=f"Sesión está {session.status}")

    parser = SLOT_PARSERS.get(slot_name)
    if parser is None:
        raise HTTPException(400, detail=f"Slot '{slot_name}' no soportado")

    data = await file.read()
    MAX_SIZE = 50 * 1024 * 1024
    if len(data) > MAX_SIZE:
        raise HTTPException(413, detail="Archivo excede 50 MB")

    # Parse first
    parsed = parser(data)
    if parsed.get("errores"):
        # Parse failed: mark error
        anexo = ict_service.update_anexo_data(
            db, session=session, anexo_code=anexo_code,
            extracted_data={}, warnings=parsed["errores"],
            uploaded_file_meta={"slot": slot_name, "filename": file.filename, "size": len(data)},
            new_status="error",
        )
        return UploadResponse(
            anexo_code=anexo_code, status=anexo.status,
            warnings=anexo.warnings or [],
            filename=file.filename, size_bytes=len(data),
        )

    # Persist raw file (will be cleaned at 24h)
    ict_service.save_uploaded_file(
        session_id=session_id, anexo_code=anexo_code,
        slot_name=slot_name, filename=file.filename, data=data,
    )

    # Map parser output to anexo extracted_data key
    extracted_key_map = {
        "f101": "f101",
        "balance": "balance",
        "kardex": "kardex_items",
    }
    main_key = extracted_key_map.get(slot_name, slot_name)

    if slot_name == "f101":
        extracted = {"f101": parsed["casilleros"]}
    elif slot_name == "balance":
        extracted = {"balance": parsed["cuentas"]}
    elif slot_name == "kardex":
        extracted = {"kardex_items": parsed["items"]}
    else:
        extracted = {main_key: parsed}

    # Determine new status: ready if all required slots present, else partial
    required = ANEXO_REQUIRED_SLOTS.get(anexo_code, [])
    existing_anexo = next((a for a in session.anexos if a.anexo_code == anexo_code), None)
    existing_files = (existing_anexo.uploaded_files if existing_anexo else None) or {}
    new_files_keys = set(existing_files.keys()) | {slot_name}
    new_status = "ready" if all(s in new_files_keys for s in required) else "partial"

    anexo = ict_service.update_anexo_data(
        db, session=session, anexo_code=anexo_code,
        extracted_data=extracted, warnings=parsed.get("errores", []),
        uploaded_file_meta={"slot": slot_name, "filename": file.filename, "size": len(data)},
        new_status=new_status,
    )
    # Recompute INDICE after any anexo change
    ict_service.recompute_indice(db, session=session)

    return UploadResponse(
        anexo_code=anexo_code, status=anexo.status,
        warnings=anexo.warnings or [],
        filename=file.filename, size_bytes=len(data),
    )
```

- [ ] **Step 3: Run + commit**

```bash
pytest tests/test_ict_router.py -v
git add backend/app/ict/router.py tests/test_ict_router.py
git commit -m "feat(ict): POST upload endpoint with parser dispatch + status update"
```

---

## Task 30: DELETE upload slot

**Files:**
- Modify: `backend/app/ict/router.py`
- Modify: `backend/app/ict/service.py`

- [ ] **Step 1: Add reset_anexo_slot to service**

```python
def reset_anexo_slot(
    db: Session, *, session: ICTSession, anexo_code: str, slot_name: str
) -> ICTAnexo:
    """Remove a slot's data + file from an anexo, recalc status."""
    anexo = next((a for a in session.anexos if a.anexo_code == anexo_code), None)
    if anexo is None:
        raise ValueError(f"Anexo {anexo_code} not found")

    files = anexo.uploaded_files or {}
    if slot_name in files:
        del files[slot_name]
    anexo.uploaded_files = files

    # Remove the slot's extracted_data key (best-effort: use same key mapping)
    extracted = anexo.extracted_data or {}
    key_map = {"f101": "f101", "balance": "balance", "kardex": "kardex_items"}
    main_key = key_map.get(slot_name, slot_name)
    if main_key in extracted:
        del extracted[main_key]
    anexo.extracted_data = extracted

    # Status: empty if no files left; partial otherwise
    anexo.status = "empty" if not files else "partial"
    anexo.last_updated_at = _now()
    db.add(anexo)
    db.commit()
    db.refresh(anexo)
    return anexo
```

- [ ] **Step 2: Add endpoint**

```python
@router.delete(
    "/sessions/{session_id}/anexos/{anexo_code}/upload/{slot_name}",
    status_code=200,
)
def reset_slot_endpoint(
    session_id: int,
    anexo_code: str,
    slot_name: str,
    user: User = Depends(require_client_with_device),
    db: Session = Depends(get_db),
):
    try:
        session = ict_service.get_session(db, session_id=session_id, user=user)
    except PermissionError as e:
        raise HTTPException(403, detail=str(e))
    try:
        anexo = ict_service.reset_anexo_slot(
            db, session=session, anexo_code=anexo_code, slot_name=slot_name
        )
    except ValueError as e:
        raise HTTPException(404, detail=str(e))
    ict_service.recompute_indice(db, session=session)
    return {"anexo_code": anexo_code, "status": anexo.status}
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/ict/router.py backend/app/ict/service.py
git commit -m "feat(ict): DELETE slot endpoint to reset a single upload"
```

---

# PHASE 4 — A2 + A3 (Tabular)

> **OUTLINE MODE**: estos anexos siguen el patrón establecido en Fase 3 (cell_map + filler + slot mapping en upload endpoint). El código completo se expandirá al iniciar la fase.

### Task 31: cell_maps/a2.py (3 cuadros con dimensiones fijas)

**Files:** `backend/app/ict/cell_maps/a2.py`

**Acceptance criteria:**
- Definir 3 dicts: `A2_CUADRO1_MAP` (8 filas × 4 cols), `A2_CUADRO2_MAP` (10 × 12), `A2_CUADRO3_MAP` (11 filas)
- Header map estándar (`razon_social`, `ruc`, etc.)
- Documentar columnas que son fórmulas (NO tocar)

### Task 32: parsers/facturacion_sri.py

**Files:** `backend/app/ict/parsers/facturacion_sri.py` + test

**Acceptance criteria:**
- Parser de reporte mensual de facturación electrónica del SRI (Excel)
- Devuelve: `{meses: {01: {emitidas, anuladas, NC}, ...}, errores}`
- Soporta los formatos exportados por "SRI en Línea" → "Reportes" → "Facturación electrónica"

### Task 33: fillers/a2_ingresos.py (Cuadro 1 — Ingresos Ordinarios)

**Files:** `backend/app/ict/fillers/a2_ingresos.py` + test (parcial)

**Acceptance criteria:**
- Lee `anexo_data["f101"]` para extraer casilleros de ingresos (6001, 6005, etc.)
- Llena matriz 8 filas × 4 columnas en Cuadro 1
- NO toca columna E que es fórmula `=B+C+D` del template
- Warnings si casilleros faltan

### Task 34: fillers/a2_ingresos.py (Cuadro 2 — IVA vs Facturación)

**Acceptance criteria:**
- Suma `anexo_data["f104_monthly"]` (12 meses agregados) para casilleros 411-499 del F-104
- Compara con `anexo_data["facturacion"]` (emitidas - NC)
- Llena las 10 filas × 12 columnas de la matriz
- Diferencias (columna M) son fórmulas → NO tocar

### Task 35: fillers/a2_ingresos.py (Cuadro 3 — Conciliación IVA→IR)

**Acceptance criteria:**
- Toma totales del Cuadro 2 + ajustes NIIF manuales
- Llena 11 filas de conciliación
- Permite que el cliente edite base legal y fundamento de hecho via UI (futuro)

### Task 36: cell_maps/a3.py (9 bloques de límites)

**Files:** `backend/app/ict/cell_maps/a3.py`

**Acceptance criteria:**
- Dict `A3_BLOQUES` con 9 entries: `gastos_gestion`, `gastos_viaje`, `gastos_indirectos`, `promocion`, `intereses`, `instalacion`, `cuentas_incobrables`, `donaciones`, `regalias`
- Cada entry: lista de (fila, casillero_F101) para llenar
- Los porcentajes (2%, 3%, 5%, 20%, etc.) ya están hardcoded en template

### Task 37: fillers/a3_costos_gastos.py

**Files:** `backend/app/ict/fillers/a3_costos_gastos.py` + test

**Acceptance criteria:**
- Itera los 9 bloques, llena casilleros base de cada uno
- NO toca fórmulas (Excel calcula porcentajes, diferencias)
- Warnings si casilleros faltan
- Test: A3 lleno desde F-101 debe match valores de ARCOLANDS (golden test)

### Task 38: Register A2 + A3 in generate_excel + upload endpoint

**Acceptance criteria:**
- Añadir `"A2": A2Filler()`, `"A3": A3Filler()` al filler_map
- Extender `ANEXO_REQUIRED_SLOTS`: `"A2": ["f104", "facturacion"]`, `"A3": ["f101"]`
- Extender `SLOT_PARSERS`: añadir `"f104": parse_f104`, `"facturacion": parse_facturacion`

---

# PHASE 5 — A4 + A7

> **OUTLINE MODE**

### Task 39: cell_maps/a4.py + fillers/a4_conciliacion_ingresos.py + test

**Files:** `backend/app/ict/cell_maps/a4.py`, `backend/app/ict/fillers/a4_conciliacion_ingresos.py`

**Acceptance:**
- Cuadro 1 (10 filas): detalle ingresos exentos. Datos del Mayor analítico (cuentas tipo 4.x.exentos)
- Cuadro 2 (5 filas): conciliación libros vs declaración (casilleros 804, 805, 812, 1112)
- Slot requerido: `mayor_exentos` + `f101` (reusa f101 ya subido en otros anexos)

### Task 40: cell_maps/a7.py + fillers/a7_credito.py + test

**Files:** `backend/app/ict/cell_maps/a7.py`, `backend/app/ict/fillers/a7_credito.py`

**Acceptance:**
- Matriz 1: Crédito IR 2022-2024 (3 filas × ~15 columnas)
- Matriz 2: ISD 2021-2025 (5 filas × ~20 columnas)
- Slot requerido: `f101_2022`, `f101_2023`, `f101_2024` (multiyear) + `f108` (ISD) + opcionalmente `resoluciones_devolucion_pdf`

---

# PHASE 6 — A5 + A6 (Business Logic)

> **OUTLINE MODE**

### Task 41-43: A5 Conciliación C&G (5 cuadros + prorrateo)

**Files:** `backend/app/ict/cell_maps/a5.py`, `backend/app/ict/fillers/a5_conciliacion_costos.py`

**Acceptance:**
- Cuadro A: detalle gastos no deducibles (filas dinámicas desde mayor)
- Cuadro B: prorrateo % ingresos exentos / total (CÁLCULO PYTHON, no fórmula template)
- Cuadro C: participación trabajadores 15% atribuible a exentos
- Cuadro D: conciliación casilleros 806, 807, 808, 809, 813, 1113
- Cuadro E: gastos no deducibles con signo negativo (reversos)
- Slot requerido: `mayor_no_deducibles` + `f101`

### Task 44-45: A6 Beneficios (3 cuadros + contratos)

**Files:** `backend/app/ict/cell_maps/a6.py`, `backend/app/ict/fillers/a6_beneficios.py`, `backend/app/ict/parsers/contratos_pdf.py`

**Acceptance:**
- Cuadro A: deducciones adicionales (casillero 810). Filas dinámicas
- Cuadro B: contratos de inversión vigentes. Datos del PDF o entrada manual del cliente
- Cuadro C: exoneraciones (Zonas Francas, deporte, exportadores habituales)
- Slot requerido: `f101` + opcionales: `contratos_inversion_pdf`

---

# PHASE 7 — A8 Comercio Exterior (XML)

> **OUTLINE MODE**

### Task 46: Extend ats_xml.py for full A8 needs

**Acceptance:**
- Parser completo: pagos al exterior CON/SIN CDI, reembolsos
- Detecta país, tipo renta, retención efectuada
- Devuelve listas estructuradas por tipo

### Task 47-49: cell_maps/a8.py + fillers/a8_comercio_exterior.py

**Files:** `backend/app/ict/cell_maps/a8.py`, `backend/app/ict/fillers/a8_comercio_exterior.py`

**Acceptance:**
- Tabla A (pagos CON CDI): filas dinámicas, columnas ~26 (RFC, país, monto, retención, certificado, etc.)
- Tabla B (pagos SIN CDI): similar
- Tabla C (reembolsos): incluye datos del intermediario
- Slot requerido: `ats_xml` + `f103` + opcional: `certificados_residencia_fiscal_pdf`

---

# PHASE 8 — Frontend ICT

> **OUTLINE MODE** — sigue patrones del portal cliente

### Task 50: ictApi.js — HTTP client

**Files:** `frontend-client/src/ict/ictApi.js`

**Acceptance:**
- Métodos: `createSession`, `getActiveSession`, `getSession(id)`, `updateSession`, `uploadAnexo`, `resetSlot`, `downloadExcel`
- Reutiliza `setToken/getToken` de `frontend-client/src/api.js`
- Maneja errores 401/403/410 consistentemente

### Task 51: ICTLanding.jsx — "Crear nueva sesión"

**Files:** `frontend-client/src/ict/ICTLanding.jsx`

**Acceptance:**
- Form con RUC, Razón Social, Ejercicio Fiscal (default 2025), Adhesivo (opcional)
- Validación: RUC formato 10-13 dígitos, RS no vacío, año YYYY
- Submit crea sesión → redirige a `/tools/ICT_2025`

### Task 52: ICTDashboard.jsx — pantalla principal

**Files:** `frontend-client/src/ict/ICTDashboard.jsx`

**Acceptance:**
- Header con datos contribuyente + botón Editar + botón Descargar Excel
- Lista de 10 cards (1 por anexo) con estado visual
- Polling cada 5s para refresh de status si hay upload reciente
- Maneja `sessionInvalidated` y `deviceBlocked` redirigiendo

### Task 53: ICTAnexoCard.jsx — card por anexo

**Files:** `frontend-client/src/ict/ICTAnexoCard.jsx`

**Acceptance:**
- Muestra anexo_code + nombre + indicador visual (⚪/🟡/✅/❌)
- Lista de slots requeridos con su estado (✅ subido / ⚪ pendiente)
- Botón "Subir archivo" por slot
- Warnings expandibles

### Task 54: ICTUploader.jsx — upload con feedback

**Files:** `frontend-client/src/ict/ICTUploader.jsx`

**Acceptance:**
- File input + botón "Subir"
- Mientras sube: spinner + barra de progreso
- Al terminar: mensaje OK / error con mensaje claro
- Permite re-subir (reemplaza el archivo)

### Task 55: ICTEditContribuyenteModal.jsx

**Files:** `frontend-client/src/ict/ICTEditContribuyenteModal.jsx`

**Acceptance:**
- Modal con form prellenado
- Submit PATCH /sessions/{id} + cierra modal + refresca dashboard

### Task 56: Wire routing + catalog link

**Files:** `frontend-client/src/App.jsx`, `frontend-client/src/catalog/ClientCatalog.jsx`

**Acceptance:**
- Nueva ruta `/tools/ICT_2025` → `ICTLanding` o `ICTDashboard` según hay sesión activa
- En catálogo, click en tarjeta "ICT 2025" navega a `/tools/ICT_2025` (no a `ToolShell` genérico)

---

# PHASE 9 — Deploy + E2E

> **OUTLINE MODE**

### Task 57: Update render.yaml (no cambios, mismas vars)

**Acceptance:** El backend ya soporta /api/v1/client/ict/*; no requiere env vars nuevas.

### Task 58: Migration verification on Render

**Acceptance:** Tras deploy, primer boot del backend ejecuta `init_db()` y crea tablas `ict_sessions`, `ict_anexos`.

### Task 59: Integration test end-to-end con ARCOLANDS golden

**Files:** `tests/test_ict_e2e_full_flow.py`

**Acceptance:**
- Fixtures: F-101, Balance, F-104, Kardex (anonimizados de ARCOLANDS 2024)
- Test: crear sesión → subir todos los inputs → descargar Excel → comparar vs ARCOLANDS lleno (tolerancia para diferencias menores)

### Task 60: Playwright E2E smoke

**Files:** `e2e-portal-cliente/tests/ict-smoke.spec.js`

**Acceptance:**
- Login cliente → navega a ICT → crea sesión → sube F-101 → ve estado actualizado → descarga Excel
- Verifica que el archivo descargado no esté vacío

---

# Self-Review

After all phases implemented, verify:

- [ ] **All tests pass**: `pytest tests/test_ict_* -v` returns no failures
- [ ] **Frontend builds**: `cd frontend-client && npm run build` succeeds
- [ ] **All 10 anexos in filler_map** (search `filler_map` in service.py — should have 10 entries)
- [ ] **All required slot parsers registered** in SLOT_PARSERS (f101, f104, f103, f108, ats, balance, mayor, facturacion, kardex, contratos)
- [ ] **Spec coverage**: every section of `2026-05-30-ict-2025-design.md` has at least one task implementing it
- [ ] **No raw secrets** committed (RESEND_API_KEY etc. stay in Render env vars)
- [ ] **24h cleanup runs** for /tmp/ict/ files (covered by cleanup_ict_orphan_files)
- [ ] **90-day session expiry** enforced (sessions in `expired` state visible)
- [ ] **All HTTP routes protected** by `require_client_with_device` (no auth bypass)
- [ ] **Golden test against ARCOLANDS** passes within tolerance
