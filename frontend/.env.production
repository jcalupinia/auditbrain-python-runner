# Producción: el frontend estático servido desde https://audit-ia.ec
# apunta al backend FastAPI, que sigue alojado en Render pero expuesto
# bajo el subdominio api.audit-ia.ec (CNAME -> *.onrender.com).
# Este valor se inyecta en build time por Vite (import.meta.env.VITE_API_BASE).
VITE_API_BASE=https://api.audit-ia.ec
