# En desarrollo dejamos VITE_API_BASE vacío para que api.js use rutas
# relativas (/api/...) y Vite las proxee al backend de Render. Así no hay
# CORS cuando se corre `npm run dev` en localhost.
VITE_API_BASE=
